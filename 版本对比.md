# TagLauncher 版本对比:1.0.0 → 1.6.3-beta

> 只对比首个正式版与当前版本,直观说明迭代变化。各功能详情见[开发手册](./PROJECT_MANUAL.md)。

## 一句话总结

1.0.0 是一个「标签 + 文件柜」的本地启动器;1.6.3-beta 已进化为**可装 Mod、可换主题、带 AI 打标、云同步与在线更新的商业级文件工作台**。1.0.0 的核心概念(标签、文件柜、收藏、启动)全部保留并增强,老用户无需重新学习。

## 体量对照

| 维度 | 1.0.0 | 1.6.3-beta |
|---|---|---|
| 源码文件数(src + src-tauri/src) | 30 | ≈176(约 5.9 倍) |
| 后端命令数 | 23 | 90(约 4 倍) |
| 自动化测试 | 0 | 300+(前端逻辑 + vitest + Rust 单元/集成) |
| 文档 | 1 个 README | 手册体系(使用/开发/教程/维护/测试计划) |
| CI / 发版流水线 | 无 | push/PR 自动测试;tag 触发 x64 + ARM64 双架构发版 |

## 能力对照

| 方面 | 1.0.0 | 1.6.3-beta |
|---|---|---|
| **找文件** | 按路径记录,文件移动/改名即丢失 | NTFS 文件 ID 作身份,同盘改名/移动自动追踪;跨盘移动按内容指纹自动找回;支持 NAS/网络盘 |
| **标签** | 平铺标签,交集筛选 | 图状标签(DAG,多父继承),选中父标签自动并入后代;关系编辑器 + 可视化标签图谱 |
| **扩展性** | 功能固定 | Mod 体系(CSS / CSS+JS / Theme,17 种权限管控、网络请求原语、列表插槽)+ 主题体系(20 套 RyuujiDesign 锁定色板内置主题、A/B 双造型风格切换、tokens/变体/自定义 CSS),附官方示例 |
| **AI** | 无 | 接入 Anthropic 协议(兼容第三方地址),一键/自动为对象智能打标;密钥只存本机、导出自动剔除 |
| **数据** | 固定在程序旁,无备份手段 | 自定义数据目录;一键备份/导出/导入;WebDAV 云备份与恢复(自动保留最近 10 份,云端副本脱敏) |
| **更新** | 无 | 启动自动检查 GitHub 新版本(24h 节流),按架构直达安装包下载 |
| **搜索** | 名称/路径/标签/拼音/同义词,150ms 防抖 | 保留全部能力 + 表达式语法、索引缓存提速、命中关键词高亮、「搜索待生效」指示、层级标签并入结果 |
| **交互** | 网格/列表 + 基础搜索 + 收藏置顶 | 大列表虚拟化、鼠标框选、命令面板(Ctrl+K)、空格快速预览、五种排序、类型筛选、批量操作、状态栏、骨架屏、主题化窗口栏、完整键盘体系 |
| **工程质量** | 无测试、无 CI | 全量自动化测试 + CI 护栏 + 契约测试(命令双向一致、版本三处一致) |

## 结论

这是一次**全方位的代际升级**:底层更可靠(文件认人不认路)、组织更聪明(图状标签 + AI)、生态更开放(Mod + 主题)、数据更安全(备份 + 云同步)、体验更现代(虚拟化 + 快捷键 + 商业级细节)。
