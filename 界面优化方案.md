# 界面优化方案

> 日期：2026-09-17（修订：纳入原始评审全文、W4 直接改标签色、补齐与评审的落差）  
> 状态：**W1–W5 已全部落地**（提交 `4b0a852` → `ddd4897`，基线 `d2334fb`），执行记录见 §11  
> 性质：决策记录 + 代码对照 + 分阶段落地  
> 来源：`宣传视频/e2e-review` 全量 62 张截图；空上下文、艺术家 && 产品设计师、三组分册独立看图后汇总  
> 对照：[文件夹链路审阅与监视方案.md](./文件夹链路审阅与监视方案.md)、[USER_GUIDE.md](./USER_GUIDE.md)、[THEME_GUIDE.md](./THEME_GUIDE.md)

---

## 0. 决策摘要

| 编号 | 议题 | 决策 | 阶段 |
|---|---|---|---|
| R0 | 欢迎页赞赏码与上手区同高，「还没帮到你先收款」 | **驳回，不改。** 赞赏纯自愿提示，项目开源，无强制 | — |
| A1 | 欢迎层压在满库上抢叙事 | 加深遮罩并虚化背景；**不改欢迎正文、不后置赞赏** | W1 |
| A2 | 空库顶栏数字与侧栏「导入后标签才出现」打架 | **有标签就列出**；文案承认「项目不在了，标签还在」 | W1 |
| A4 | 第一屏没有「现在在看什么」 | 主区加范围标题（全部 / 收藏 / 娱乐·含下级 / 2 张图片）；卡片补选中与打开语言 | W2 |
| B1 | 筛选条默认收起 | **有库时默认展开**；视图切换补可见短字 | W2 |
| B2 | 「仅名称」徽章与「名称」页签双控件 | 筛选行可见时不画徽章；`clearWorkspaceFilters` 同时复位 `searchMode` | W2 |
| B3 | 父标签含后代、多选交集不可见 | 侧栏按代数浅缩进（最多两级）+「含下级」；芯片之间写「且」。**不做可折叠整树** | W2 |
| B4 | 1–3 条结果像货架被搬空 | 少结果做成居中一组，主区标题写清「N 项 · 已筛 ×」 | W2 |
| C1 | 批量移除把删盘做成最右实心主按钮 | **仅出库 = 主按钮**；删盘 = 描边危险。从菜单「删本地」进来时才强调删盘 | W1 |
| C2 | 数据管理六颗等权胶囊 | 按风险分组 | W1 |
| C3 | 失效找回失败只活在角落 toast | 主按钮改为「尝试找回」；失败写在该行上 | W1 |
| D1 | 预览 / 命令面板几乎无遮罩 | 改走 `--overlay-bg`；打开时收起或压暗批量条与底栏 | W3 |
| E1 | 四种预览像同一张属性卡 | **四种不同的主体布局**，不是在同一模板上打补丁 | W3 |
| F1 | 图谱默认无选中 → 三排糖豆 | 打开即选中有孩子的节点并画线；换行，禁止半颗豆 | W3 |
| G1 | 四套配色名大于画面差；素墨仍是彩虹糖豆 | **拉开纸色，并直接改库里的标签色 / 文件柜色**（按色位写入 `color`） | W4 |
| G2 | 设置里主题下拉 + 预览卡重复 | 预览卡作主选择器；下拉留给自定义 / Mod | W4 |
| G3 | 侧栏主题色点像在展览换肤 | **色点保留**；常驻教程改为首次提示，可关掉 | W2 |
| H1 | 设置抽屉漏出半截真卡片 | 主题页保留实时预览；其它区块加强压暗 | W4 |
| I1 | 子菜单焦点错位；批量加标签无已有态 | 高亮跟父项；菜单显示全有 / 部分有 | W4 |
| J1 | 空态 / 快捷键 / AI 进度偏说明书 | 空态点名冲突；快捷键可搜；AI 进度露出当前对象 | W5 |

一句话：赞赏区按作者裁定原样留下。旧稿把评审收成「纠错 + 换 token」，视觉上仍是同一套产品；本稿按评审原句抬回「第一眼身份、四种预览仪式、四间房间」，其中房间靠**直接改标签色**落地，不用滤镜假装换肤。

---

## 1. 对「计划能不能大幅提升」的回答

上一版（只加深遮罩、侧栏加两个字、素墨用 `color-mix` 降饱和、预览只补失败态皮）**达不到**空上下文评审要的那一档。

| 评审要的 | 旧稿实际交付 | 观感 |
|---|---|---|
| 第一眼是启动器，不是漂亮文件墙 | 欢迎加一层模糊 | 还是同一面墙 |
| 搜和筛是一层可学会的表面 | 默认展开 + 去掉重复徽章 | 交互会好，脸还是旧的 |
| 覆盖层分声部 | 把 `color-mix` 换成 `--overlay-bg` | 有一点，戏剧性不够 |
| 预览四种仪式 | 在同一对话框里打补丁 | 并排看仍像换壁纸 |
| 四套是四间房间 | 纸色微调 + 渲染层降饱和 | **素墨截图仍会是灰纸 + 彩虹糖豆** |
| 危险按钮推荐方向 | 仅出库变主按钮 | 这是信任，不是视觉跃迁 |

旧稿偏了，是因为把 KISS 用成了「凡是改构图、改房间、写回颜色的都降级」。评审原句要的不是更干净的组件库演示，而是：

> 已经能卖「干净、可控、像一套库」。还不能卖「我离不开的启动器」——差的不是像素，是第一眼的身份、筛选的可学性，以及少结果时的构图勇气。

本稿把视觉跃迁收成三件必须看得见的事，其余信任项仍做，但不冒充「大幅提升」：

1. **第一屏会说话**：范围标题 + 默认展开的搜筛 + 少结果重新构图 + 浅缩进的标签关系。  
2. **打开一层就是另一层**：遮罩够暗、底栏让位；图 / 声 / 影 / 夹四套主体。  
3. **换主题等于换房间**：纸色拉开，**标签和文件柜的 `color` 按色位写进库**。切到素墨，糖豆必须变成墨阶；切回樱花，按同一色位回到春色。

做完这三件，空上下文再看 `02` `25–28` `53–62`，应能看出不是同一张皮换强调色。只做 W1 的信任修复，观感不会变。

---

## 2. 原始评审意见（背景，空上下文只看图）

以下是三组分册独立评审的原文汇总，**先于代码校正**。实施时以第 0 节决策为准；本节省得对照「评审要什么」。赞赏相关句子保留，作为已被驳回的原文，见 §3.2。

### 2.1 总第一印象

第一眼是一套很干净的浅色桌面工具：白卡片、大圆角、粉彩标签胶囊、圆角色块图标，像「个人资料库」多于「资源管理器」。气质克制、整齐、偏近年效率软件，几乎没有装饰噪音，也几乎没有品牌温度——换成另一个名字，画面仍然成立。

主界面一上来就是满库网格，欢迎层叠在已经填满的工作区上，产品身份（本机启动器）要靠读卡片路径和侧栏标签才能拼出来，不是靠一个瞬间被记住。工具感强、仪式感弱：能感到「能管很多东西」，不容易感到「这是我每天打开东西的地方」。整体完成度明显高于普通开源桌面壳，但美是平均的美，记忆点还没长出来。

覆盖层几乎全是「白浮层直接压在仍完全可读的主界面上」，礼貌到近乎不敢抢戏。命令面板、预览窗、对话框、右键、图谱用的是同一副嗓音。预览里最动人的是那张日落图——大图当主角，元数据退到脚注；音频和视频却立刻露出模板感。

设置不是独立偏好窗，而是盖在资料库上的第二张书桌——左侧导航和半截卡片始终在场。危险路径（失效复核、批量移除）比多数启动器认真，文案也在解释后果；但视觉仍把最重的那一刀放在最像「主按钮」的位置。四套诗意配色名很美，画面上却多是换了一层画布温度，只有樱花真正改了房间的季节。空态礼貌、骨架结构正确，二者都偏安静，安静到几乎不像在招呼第一次打开的人。

### 2.2 评审认为做得好的

- 卡片作为主物件，色块图标 → 标题 → 路径/类型 → 标签胶囊，网格和列表是同一套语法。  
- 标签色点与胶囊形成稳定识别；筛选后芯片回显，过滤不是黑箱。  
- 欢迎页「三步上手」结构是对的：加东西 → 打标签 → 搜开。  
- 入库弹窗把文件夹当成决策，而不是静默吞盘。  
- 失效被当成库的健康问题：卡片黄签和底栏「待处理」前后呼应。  
- 命令面板「输入即收束」干净，底栏用「命令 / 项目」分开两种结果。  
- 图片预览把情感放对了位置，像一次正经的 Quick Look。  
- 右键分段清楚，危险项单独成组并改成红色；多选时数量写进动词。  
- 批量移除把「仅出库」和「删除本地文件」拆开，并红字点出文件夹进回收站。  
- 主题把「配色方案」和「亮 / 暗 / 跟随系统」分开；扩展卡上的权限胶囊少见地诚实。  
- 空库中心文案主动说明「文件柜只分组、不搬磁盘上的文件」；骨架屏的卡片解剖和真卡片对齐。

### 2.3 分区：主界面与检索（01–21）

**第一印象（本区）**  
欢迎窗叠在已经排满的库上；窗内三步上手和赞助区几乎同高。默认主界面只有一条搜索框和一排无字图标，搜范围和类型要再点才长出来。产品最强的能力在第一屏是隐形的。

**问题（按评审原优先级）**

1. **高 · 01 欢迎页** — 一边说从零开始，一边展示成熟资料库。赞赏区和上手区抢层级，情感上被读成「还没帮你，先收款」。  
2. **高 · 02 网格（对照 08、12）** — 搜索范围和类型默认隐藏。顶栏像浏览器插件条，新用户能学会的只有「看卡片」和「点侧栏」。  
3. **高 · 08–10 搜索模式** — 「仅名称」徽章和「名称」页签重复；拼音、表达式与日常关键词平级；`&&` 出现在温柔界面里像换了一套产品。  
4. **高 · 16–17 标签筛选** — 点「娱乐」会带出电影/游戏，侧栏却仍是平级清单。多选「开发 + 自动化」没写是「同时满足」还是「任一即可」。  
5. **中 · 05/06/20 文件柜** — 新建窗与新建标签同一张版式；柜的选中行和标签选中行长得一样。  
6. **中 · 03 入库方式** — 一次添加被做成技术三选一，视觉重量在免责声明上。  
7. **中 · 07/13/14 少结果** — 一两张卡片贴左上，数量只活在底栏小字，像货架被搬空。  
8. **中 · 12–15 类型筛选** — 选中态弱；空态不点名「开发里没有图片」。  
9. **中 · 02/04/18/19** — 网格/列表缺少当前焦点；最近使用看起来像全库；收藏/柜没有内容顶标题。  
10. **中 · 侧栏底部** — 主题色点和常驻拖拽教程占住导航。  
11. **中 · 11/15 空态** — 结构对，表情是系统对话框。  
12. **低 · 04 列表** — 类型列偏空；行与行靠呼吸分开，密度一高会粘连。  
13. **低 · 21 排序** — 「智能」无解释；菜单压住卡片。  
14. **低 · 05/06 色名条** — 颜色被做成全宽表单题。  
15. **低 · 文案口音** — 同一张脸用了教师、运维、极客三种口音。

**本区原 5 条优先建议**

1. 重写第一屏故事：欢迎不要压在满库上；先完成一次「加文件」，赞赏后置。  
2. 把搜和筛收成一层可学会的表面；拼音/表达式降为进阶。  
3. 让标签树和「且/或」长在界面上；文件柜做出和标签不同的物件。  
4. 筛选态、选中态、稀疏散布要响；数字从底栏搬到内容区。  
5. 入库从「技术三选一」改成「一个推荐 + 其他方式」；侧栏教程让路。

### 2.4 分区：覆盖层与操作（22–37）

**第一印象（本区）**  
覆盖层礼貌到不敢抢戏。视频预览是海报。图谱打开后是三排糖豆，没有连线，也没有被选中的关系。整体像一套完成度很高的浅色组件库演示，overlay 的戏剧性和关系的叙事都还没长出来。

**问题（按评审原优先级）**

1. **高 · 22/25/30/36/37** — 白卡片浮在满对比主界面上，几乎没有遮罩。底栏「已选中 / 从库中移除」会从预览窗底下露出来。  
2. **高 · 27 视频预览** — 没有播放键、进度条、时间码，是「看属性」不是「预览视频」。  
3. **高 · 37 图谱** — 名字叫图谱，眼睛看到的是库存货架。第 2 排被裁出右边缘，第 3 排半颗空豆。  
4. **高 · 31 文件柜子菜单** — 焦点蓝框还套在「打开」上，子菜单像掉在网格里的便利贴。  
5. **高 · 35 批量加入标签** — 十一张卡全选后，孤菜单无搜索、无勾选、无「部分已有」。  
6. **中 · 26 音频** — 时间写三遍，播放条却是 `0:00/0:00`。  
7. **中 · 28 文件夹** — 先属性表，每行两个按钮，脚上第三个「打开」。  
8. **中 · 30 标签编辑器** — 均匀点阵，已选不够亮；底栏仍露着「1 已选中」。  
9. **中 · 36 关系编辑器** — 左右都是圆片；只有「完成」，没有取消。  
10. **中 · 33/34 全选** — 十一道描边均分权重；底栏像状态条不像指挥台。  
11. **中 · 29/32 多选右键** — 第一句仍是单数「打开」。  
12. **低 · 22 命令面板** — 未分组的功能清单。  
13. **低 · 24 对象搜索** — 高亮行吃掉类型身份。  
14. **低 · 25 与 27 并置** — 图和视频同一套插画母题。

**本区原 5 条优先建议**

1. 先给覆盖层分声部：菜单无遮罩、面板轻遮罩、对话框中遮罩、图谱整幅幕布。  
2. 把快速预览按媒介重写成四种仪式。  
3. 所有二级菜单必须长在父项上；批量加标签要显示已全有 / 部分有。  
4. 图谱先表达关系再表达库存；做不到就改名。  
5. 压住全选之后的视觉通胀；写清「从库中移除 ≠ 删除本地文件」。

### 2.5 分区：设置、系统态与主题（38–62）

**第一印象（本区）**  
设置盖在资料库上的第二张书桌。危险路径文案认真，视觉却推荐毁盘。四套诗意配色名很美，只有樱花真正改了房间的季节。素墨名不副实：墨要收，糖豆却在喊。藤色几乎是霜靛的近亲。

**问题（按评审原优先级）**

1. **高 · 50 批量移除** — 最右实心粉键带垃圾桶，阅读习惯落在毁盘路径上。列表里看不出哪几项是文件夹。  
2. **高 · 51 空库** — 画布「暂无项目」，顶栏仍是「0 项目 / 19 标签 / 2 文件柜」。  
3. **高 · 38–46 设置** — 中间半截卡片不响应任何设置项；更新页配不上单独一页；数据六键同一体重。  
4. **高 · 43 数据管理** — 整库生死页长得像工具条。  
5. **中 · 53–62 四套配色** — 名字卖四间房间，画面卖换强调色。暗色标签对比不足。  
6. **中 · 38/39 主题选择** — 下拉和预览卡做了两遍；素墨掉到第二行。  
7. **中 · 48/49 失效** — 「找回」带着警告三角；失败只活在角落吐司。  
8. **中 · 47 快捷键** — 说明书不是速查表，无搜索。  
9. **中 · 41/42 AI 打标** — 开始瞬间是全零收据；结束只有关门。  
10. **中 · 52 骨架** — 对比太低，侧栏提前写「暂无」。  
11. **中 · 40 AI 设置** — 「为全部打标」看起来只是另一颗设置按钮。  
12. **低 · 44 云同步** — 五键赛跑。  
13. **低 · 55/56 列表** — 类型列浪费扫视宽度；暗色失效难看见。  
14. **低 · 46 扩展** — 卸载与启用并排。

**本区原 5 条优先建议**

1. 先修危险按钮的推荐方向：出库为主、删盘为次。  
2. 让设置成为唯一舞台。  
3. 空态只讲一个故事。  
4. 让四套配色配得上名字：藤色拉开纸色，素墨收回糖豆，暗色补标签对比。  
5. 把进行中的事说完：快捷键可搜，AI 看见当前对象，失效失败写在行上。

### 2.6 评审原 7 条总优先（当时给作者的合并清单）

1. 重写第一屏故事。欢迎不要压在满库上；空库的数字、侧栏、中心 CTA 对齐；赞赏后置。让人 5 秒内说出：这是用来找和打开本机东西的。  
2. 先修危险按钮的推荐方向。出库为主、删盘为次。  
3. 把搜和筛收成一层可学会的表面。去掉双控件；父子可见，多选写明「且」。  
4. 给覆盖层分声部。预览打开时收起底栏。  
5. 按媒介重写快速预览。尤其把视频从海报改回播放器。  
6. 图谱先表达关系再表达库存。  
7. 让四套配色配得上名字。筛选后 1–3 个结果重新构图。

**作者裁定对第 1 条的修正：** 「赞赏后置 / 先收款」整段驳回，见 §3.2。第 1 条其余部分（欢迎压在满库上、空库数字打架、5 秒身份）仍采纳，用遮罩虚化 + 范围标题落地，不改赞赏区。

---

## 3. 来源与裁定

### 3.1 评审怎么来的

`npm run demo:shots` 巡演 62 张图，三名互不共享上下文的评审只看图：01–21 主界面与检索，22–37 覆盖层与操作，38–62 设置与主题。§2 是他们的原文汇总。

### 3.2 唯一驳回

评审写：「赞赏码和上手区几乎同高。第一眼叙事自相矛盾——还没帮到你，先收款。」以及总优先第 1 条里的「赞赏后置」。

**驳回。** 作者裁定：赞赏是纯提示，无强制、无拦截主路径；项目纯开源；实际上没有人赞赏。

因此欢迎页赞赏区的位置、面积、与三步上手的相对高度一律不动。不得以后置、折叠、降级、挪进设置为名改这块。欢迎页其它改动（遮罩、虚化）不得顺手动赞赏 DOM。

### 3.3 看图走眼、目标仍采纳

| 评审看到的 | 代码实际 | 仍要改的 |
|---|---|---|
| 图谱没有连线 | `TagGraphView` 在 `selectedNodeId !== null` 时画线；默认 `null` | 打开即选中有孩子的节点 |
| 视频预览没有播放器 | 已有 `<video controls>`；demo 路径失败走静帧 | 失败态也要像播放器；视频主体与图片主体必须不像同一模板 |
| 所有浮层都没有遮罩 | 确认框 / 设置 / 快捷键 / 图谱已用 `--overlay-bg`；预览与命令面板用浅 `color-mix` | 后两者改走同一套遮罩，并收起底栏 |

### 3.4 标签色为什么必须写回库

主题里已经有 `--tag-preset-colors`。素墨的预设是墨阶，霜靛 / 藤色 / 樱花是彩色。但标签和文件柜的颜色存在 SQLite 的 `color TEXT` 里，演示数据还是 `#3b82f6` / `#ec4899` 这一盘彩虹。换主题只换纸，不换糖豆——所以 61 素墨截图名不副实。

旧稿用渲染层 `color-mix` 降饱和，是假装换房间。作者裁定：**W4 直接改标签色**。切到某个官方家族时，把每条标签、每个文件柜的 `color` 写成该家族当前亮暗板的对应色位。切走再切回来，靠色位还原，不能靠滤镜。

---

## 4. 原则

1. **语义冻结。** 仅出库 ≠ 删除本地文件；文件柜 ≠ 磁盘目录；导入三档保持，见文件夹方案 D。  
2. **赞赏冻结。** 见 §3.2。  
3. **文案单点。** 对外短句进 `emptyStateCopy.ts`、`itemActionCopy.ts`。  
4. **主题 id 冻结。** 四族 UUID 不换。  
5. **标签色写回。** 官方家族（含亮暗切换）按色位更新 `tags.color` 与 `cabinets.color`。自定义 / Mod 主题不批量改用户色。  
6. **构图可以改，子系统不新开。** 范围标题、少结果改组、预览四种主体、侧栏浅缩进都做。不做表达式可视化编辑器，不做可折叠整树，不做新组件库。  
7. **信任与房间都要交，但不要互相冒充。** W1 保住库；W2–W4 才是评审要的脸和房间。

---

## 5. 现状诊断（对着代码）

### 5.1 第一屏

- `WelcomeModal` 遮罩只有约 38% 的 `--overlay-bg`，无虚化，底层网格可读。赞赏 `aside` 不碰。  
- `workspaceFiltersOpen` 初值 `false`。有库时第一眼没有范围和类型。  
- 空库时 `Sidebar` 用 `allItems.length === 0 ? [] : tags`，再画「导入项目后，标签会出现在这里」，顶栏同时报 `tags.length`。  
- 主区没有「当前范围」标题。收藏 / 最近 / 文件柜 / 标签筛选只靠侧栏高亮。  
- 侧栏底：官方四色点保留；常驻 `sidebar-hint` 改为可关。

### 5.2 搜与筛

- 范围页签和「仅名称」徽章会同时出现。  
- `filterItemsByTags` 已按 DAG 闭包并入后代；侧栏扁平，计数是直接打标数。  
- `TagFilterBar` 多枚芯片并排，不写「且」。  
- `clearWorkspaceFilters` 不重置 `searchMode`。

### 5.3 危险操作

- 批量移除：仅出库是普通键，删除本地文件是最右 danger。  
- 数据管理六键一排。  
- 失效找回失败只 `showToast`；`StatusBar.spec.tsx` 未覆盖 `recovered === 0`。

### 5.4 覆盖层与预览

- 预览 / 命令面板用 `color-mix(bg-base 70–72%)`。  
- 四种媒介共用同一页眉页脚。音频时长写多遍。视频失败回退静帧。文件夹先属性后目录。

### 5.5 图谱

- 有连线算法，默认不选中。宽层裁切，可出现半颗豆。

### 5.6 主题与标签色

- `--tag-preset-colors` 已按家族分盘，素墨已是墨阶。  
- `tags.color` / `cabinets.color` 是写死的 hex。演示标签全部是 Tailwind 饱和色。  
- 新建标签从当前 `--tag-preset-colors` 随机取色，**已有标签不会跟着换房**。  
- 设置抽屉漏出半截真卡片；主题下拉与预览卡重复。

---

## 6. 方案分册

### A. 第一屏（赞赏不动）

**A1 欢迎遮罩**  
只改遮罩：`--overlay-bg` 略加深 + `backdrop-filter`（新 token `--overlay-blur`，默认 8px）。不改 `STEPS`、赞赏、按钮。验收：底层卡片不可读；赞赏区相对位置不变。

**A2 空库承认标签还在**  
去掉空库藏标签。`emptyStateCopy("library")` 在仍有标签或柜时补一句「标签和文件柜还在」。标题仍是「暂无项目」。

**A3 教程可关**  
`sidebar-hint` 首次或可关闭；拖拽进行中的释放提示保留。色点不拆。

**A4 范围标题与选中语言**  
主区顶部写当前范围，例如「全部项目」「收藏夹 · 4」「娱乐 · 含下级 · 4」「图片 · 2」。网格/列表必须有当前焦点（浅底或细环）。这是「5 秒内说出这是启动器」的主落点，不是加一句 slogan。

### B. 搜与筛

**B1** 有库时筛选行默认展开。已写入的用户偏好仍尊重。视图切换在宽度足够时带短字。  
**B2** 筛选行可见时不渲染范围徽章。清空筛选同时复位 `searchMode`。拼音 / 表达式 chip 保留。  
**B3** 按 `buildChildrenMap` 做最多两级浅缩进；有子的行标「含下级」。`TagFilterBar` 芯片之间插入不可点的「且」。不做展开折叠，避免「计数含不含下级」的新语义。  
**B4** `items.length <= 3` 时居中收成一组，标题写清数字和已筛条件。筛选空态点名冲突。  
**B5** 入库三档语义不变；默认项保持大卡，另外两张先收成一行。

### C. 危险操作

**C1** `preferDeleteFiles === false` 时仅出库为 primary，删盘为描边 danger。从菜单删本地进来时才实心强调删盘。列表标出文件夹。标题可用「移出资料库」。  
**C2** 数据页分安全 / 位置 / 危险三块。  
**C3** 找回为 primary，去掉该按钮上的警告三角。`recovered === 0` 写在行上。补测试。

### D. 覆盖层分声部

预览、命令面板改 `--overlay-bg`（可略深于欢迎，但不能比设置抽屉更抢）。打开时批量条隐藏或 `inert`。菜单保持无遮罩。图谱保持整幅幕布。z-index 数值不重排。

### E. 预览四种主体

不要共用一块「媒体 + 同一套 dl + 同一对页脚按钮」硬套四次。

- **图**：大图 bleed，脚注元数据。现状接近，保持。  
- **声**：封面或波形皮肤；时长只出现一次，写成 `0:00 / 4:29`。  
- **影**：16:9 播放框是主体。有片源用控件；失败仍是播放器皮（大播放钮禁用 + 时间轴 + poster），禁止退回一张无铬静帧。  
- **夹**：目录是主体；属性缩成一行；行内操作悬停；脚上只留「打开此文件夹」。

### F. 图谱

打开时选中子节点最多的标签并画线。宽层换行，禁止半颗豆。空关联节点降对比。双击筛选语义不变。

### G. 房间：纸色 + 直接改标签色

**G1 纸色**  
藤色提高纸色与描边的紫含量。樱花让分割线沾一点春色。暗色标签 chip 加实底，保证字和底分开。预览卡必须能看出纸色，四卡 2×2。

**G1b 直接改 `color`（作者裁定）**

标签色是色位 0–7，对应当前官方家族的 `--tag-preset-colors`，不是一张永远随身的纪念 hex。

1. 首次进入官方家族、或在官方家族之间切换（含亮暗）：  
   - 已有色位记录则用该位；  
   - 否则在**上一张官方板**里找最接近的下标，记成色位；  
   - 找不到（自定义色）则按色相 / 明度落到新板最近位。  
2. 用新板的 hex **调用已有 `update_tag` / `update_cabinet` 写回**。不要只改 CSS。  
3. 色位图存设置（如 `taglauncher.color_slots`：`{ tags: { [id]: slot }, cabinets: { [id]: slot } }`），保证素墨 → 樱花能按位回到彩色，而不是从一盘灰里猜色相。  
4. 一次切换走**一条批量 IPC**（新命令或现有批量更新），禁止 19 次来回。  
5. 自定义 / Mod 主题：不批量改用户色；编辑器仍用该主题自己的 `--tag-preset-colors` 给新建项上色。  
6. 用户在编辑器里手选某一色：更新该条 hex，并重写它的色位。  
7. 演示数据可继续用饱和 hex 种子；主题巡演一旦切家族，必须写出对应板的色，这样 `61-theme-素墨` 的糖豆是墨阶。

落点：`src/lib/tagColors.ts`（色位映射纯函数 + 测试）、主题应用处（`ThemeProvider` / `useTheme`）、`tag_service` 或新批量命令、`useTags` / `useCabinets`。文件柜与标签同一套色位，避免柜还在喊、签已经墨了。

**G2** 官方四族以 Gallery 为选择器。  
**G3** 侧栏色点保留。

### H. 设置舞台

非主题区块加深遮罩或让中列 `inert`。更新页填满版本与检查结果，不并入欢迎。AI：「未打标」为主，「全部重打」降级。

### I. 菜单

子菜单高亮停在父项。批量加标签写「将加到 N 个对象」，全有实心勾、部分有 `3/11`。多选右键第一项写「打开 N 项」。

### J. 口音（W5）

快捷键顶栏搜索。AI 进度露出当前文件名。新建色名条改色点阵；文件柜新建强调用途。

---

## 7. 阶段

| 阶段 | 内容 | 离开时应能在截图里看见 |
|---|---|---|
| W1 信任 | A1 欢迎遮罩、A2 空库列标签、C1–C3 危险路径 | 误删不再被推荐；空库不再撒谎；找回失败在行上。**脸还不会变。** |
| W2 身份 | A4 范围标题、B1–B5 搜筛与少结果、A3 教程、B3 浅缩进 | `02` 看得出在搜/筛；`13/14` 不再是左上孤卡；侧栏看得出娱乐含下级 |
| W3 仪式 | D 遮罩、E 四种预览主体、F 图谱默认连线 | `25–28` 四种不像同一页；`37` 第一眼有父子线 |
| W4 房间 | G 纸色 + **写回标签/柜颜色**、H 设置、I 菜单 | `61` 素墨是墨阶糖豆；`57` 藤色纸色不同于霜靛；切回樱花色位还在 |
| W5 口音 | J | 不引入新语义 |

W4 必须跑四族 × 亮暗截图，并抽查：切素墨后库里的 `tags.color` 确实变成墨阶 hex，再切樱花按色位回到彩色。只看渲染、不看库，不算完成。

---

## 8. 测试与截图

- 色位：`tagColors` 增加「旧板 hex → slot → 新板 hex」往返测试；素墨板上不应残留饱和色（自定义主题除外）。  
- 逻辑：`emptyStateCopy.design.test.ts`、`StatusBar.spec.tsx`（`recovered === 0`）、`TagFilterBar.spec.tsx`（「且」）、筛选默认与清空复位 `searchMode`。  
- 契约：`data-workspace-overlay` 清单不减；z-index 相对顺序不改。  
- 巡演：欢迎仍断言赞赏码可见。网格能直接看到筛选行和范围标题。图谱在拍摄前已有默认选中连线。视频断言改为「播放器皮或 `<video>`」。空库侧栏看得到已有标签。素墨主题截图的标签胶囊必须是墨阶。  
- 每阶段结束跑 `npm run demo:shots`，用空上下文看图。W2 / W3 / W4 结束时应能分别对上 §1 的三件看得见的事。

---

## 9. 明确不做

- 移动、缩小、折叠、后置欢迎页赞赏码或「赞助开发者」。  
- 改变导入三档语义，或给 CLI / MCP / Mod 弹入库窗。  
- 默认监视文件夹；猜测「人删了」和「盘拔了」。  
- 更换四族主题 UUID。  
- 在自定义 / Mod 主题下批量改用户标签色。  
- 侧栏可折叠整树、表达式可视化搭建器。  
- 把「更新」并进欢迎 / 关于。  
- 拆掉侧栏官方主题色点。  
- 用渲染层滤镜代替写回 `color`（旧稿 G1，已废止）。  
- 为优化单开新设计系统。

---

## 10. 建议的第一刀

W1 仍先做信任，避免房间还没换先把库弄丢：

1. `Sidebar.tsx` — 空库列出已有标签。  
2. `RemoveFromAppConfirmDialog.tsx` — 仅出库变主按钮。  
3. `DataSettingsSection.tsx` — 按风险分组。  
4. `MissingItemsReviewDialog.tsx` + `StatusBar.tsx` — 找回为主，失败上列表。  
5. `WelcomeModal.tsx` — **只改遮罩**。

随后 W2 范围标题与少结果、W3 四种预览、W4 色位写回。不要指望 W1 单独交出评审要的视觉跃迁。

---

## 11. 执行记录（2026-09-17）

主进程制定任务包并派发子代理（Grok 4.6）并行实现，每阶段一个提交；每阶段闸门 = `tsc` + `npm test`（逻辑测试 + vitest）+ `cargo test` + `npm run demo:shots`（Playwright 断言 + 62 张截图），再由主进程看图复核。W1–W4 完成后另派独立子代理只读复核，P1 问题已在 W5 提交中修复。

| 阶段 | 提交 | 落地内容 | 闸门 |
|---|---|---|---|
| 基线 | `d2334fb` | 文件夹导入二选一 / 回收站 / 失效复核 + 两份方案文档 | tsc ✓ · vitest 90 · cargo 139 · e2e 69/69 |
| W1 信任 | `4b0a852` | A1 欢迎遮罩 `--overlay-blur` 8px、`--overlay-bg` 0.38→0.46；A2 空库列出标签、空态文案按实际有无标签/柜拼句；A3 侧栏教程可关（`taglauncher.sidebar_hint_dismissed`）；C1 「仅出库」primary、删盘 danger 描边并挪到最左、Enter 永不落删盘；C2 数据页安全/位置/危险三组；C3 找回 primary、`recovered===0/>0` 写在行上 + toast | e2e 71/71 |
| W2 身份 | `3d20118` | A4 `WorkspaceScopeHeader`（`resolveWorkspaceScope` 纯函数）；B1 筛选行默认展开并进 `workspace_prefs`、视图切换短字；B2 筛选行可见不画徽章、清空复位 `searchMode`；B3 `flattenTagTree` 两级浅缩进 + 悬停「含下级」、芯片间「且 / 且非」；B4 ≤3 项居中成组；B5 入库对话框推荐大卡 + 两档一行 | e2e 76/76 |
| W3 仪式 | `f745c81` | D 预览 / 命令面板改 `--overlay-bg`，批量条 `suppressed` 让位；E `preview/` 四主体（图 bleed / 声条形播放器 `0:00 / 4:29` / 影 16:9 失败仍播放器皮 / 夹目录为主体）；F `pickDefaultGraphNode` 打开即连线、层内换行、非关联降对比；I 多选右键「打开 N 项」 | e2e 76/76 |
| W4 房间 | `f39bcd4` | G1b Rust `recolor_tags_and_cabinets` 单事务批量、`tagColorSlots.ts` OKLab 最近色位 + `taglauncher.color_slots` 记忆、`useTagColorSlotSync` 切官方家族写回库；G1 藤色/樱花纸色拉开、暗色 chip 实底 30%；G2 `ThemeFamilyGallery` 2×2 主选择器、下拉只列自定义/Mod；H 非主题区遮罩加深虚化、更新页填满、AI「未打标」为主；I 子菜单焦点跟父项、批量加标签「将加到 N 个对象」+ 全有勾 / `k/N` + 过滤框 | e2e 80/80（含素墨下卡片胶囊与侧栏色点均为墨阶） |
| W5 口音 | `ddd4897` | J 快捷键可搜、AI 进度显示 `currentName`（对象名）与完成总结、色名条改色点阵、文件柜窗写用途；复核修复：色位同步首次进入官方家族且数据到齐才写回、空目录不覆盖色位图、IPC 失败不持久化、下拉「使用上方官方配色」真正切回 | tsc ✓ · 逻辑 225 · vitest 118 · cargo 139 · e2e 80/80 |

巡演脚本同步：移除确认框按 `/^移出资料库/` 定位并断言唯一实心主按钮；搜索范围只认页签；范围标题 / 「且」 / 含下级 / Gallery / 素墨墨阶均有断言。

**留给发版（F09）的文档债**：USER_GUIDE.md（视频失败态、「为全部对象打标」、图谱默认选中、芯片「且」、主题 Gallery 与色位写回）、PROJECT_MANUAL.md（视频回退、TagFilterBar）、THEME_GUIDE.md（`tag-preset-colors` 会改已有标签/柜的库内 `color`）。

**已知取舍**：手选标签色不立即重写色位，靠 `record.hex !== current` 在下次切官方家族时按旧板重算；少结果分支换行时方向键仍按单行。`scripts/verify-layout.mjs` 已改走 Gallery（2026-09-17 复验轮）。

---

## 12. 第三轮评审全量落地（2026-09-21，W6 + P0/P1/P2）

第三轮空上下文评审（`_评审报告汇总-第三轮.md`，66 张截图、3 名评审代理）共产出 2 个 P0、约 17 个 P1、约 27 个 P2，作者裁定**全部做**，并裁决两处评审分歧：列表表头改白底 + 下边框（不再用米色纸感带）；主题入口合并为一个（删标题栏月亮/太阳，明暗唯一入口 = 侧栏 Dock「浅色/深色」分段，「跟随系统」仍在设置页）。

### W6 主题 / 深色专项（根因修复，先于一切）

| 项 | 根因 | 落地 |
|---|---|---|
| 切亮暗「特别奇怪」的最大单一来源 | 霜靛·亮板（sakura.ts）8 色位沿旧八族序，与暗板错位——默认家族每次切亮/暗标签色相互换（绿↔粉、青↔白） | 亮板槽序对齐工厂新语义（主色/跨族A/跨族B/中性/琥珀/绿/橙/蓝），蓝位统一取亮色板 info `#1f8ad8`；`tagColorSlots.test.ts` 补「全家族亮板↔暗板同槽 OKLCH 色相 ≤30°」用例（旧往返测试查不出错位） |
| 切换过渡整体硬跳 | `theme-switching` 类双 rAF（16-34ms）即移除，按 CSS 规范运行中的过渡被取消 | 改 320ms 计时器（覆盖 `--transition-normal` 240ms）后移除，连续切换重置计时，240ms crossfade 真正生效 |
| 切主题后网格静默回弹 | `applyTheme` 删-写循环吞掉 `applyCardSizeVars` 写入的 `--grid-col-min` | 写入前快照、写入后恢复 `grid-col-min / card-thumb-size / card-text-scale / grid-col-min-icons` |
| 深色边框线不和谐 | 硬编码边框≈0，问题在 token 层四套并存线配方（hairline 72% / border-subtle 白 13% / border-default 染色 / 行分隔暗色 92% 提浓）+ 暗色卡片白 8% 顶唇三层边缘 | A 发丝边按亮暗分叉：暗色 `border-subtle/medium` 改从 `border-default` 向 text 提亮派生（同色温）；删行分隔暗色提浓；暗色顶唇 8%→4%；`WelcomeModal` 赞赏码 `bg-white` 保留（二维码可扫性必需） |
| 暗色面层级过扁 | 暗色卡面与画布明度差过小（素墨暗最明显） | 4 家族 dark `surface/surface_2` 抬明度一档（素墨 `#1e1e1e`→`#262626`） |
| 调色级 | — | 素墨 hairline 掺 12% 墨/纸（亮/暗），白卡不再只靠投影；樱花亮画布降饱和（bg 提亮 + PAPER_BIAS.paper 6→3）；藤色暗 accent 向暖紫推 ~12°（`#a855d1`），与霜靛暗拉开，TUNED 登记 |
| 死代码 | `COLOR_MODE_CHANGED_EVENT` 声明后从未派发 | 删除 |

### P0（2 项）

- **大图标视图占位图重复**：demo 占位图改按对象类型分图形语言（风景/胶片/均衡器/文件夹/应用窗口/终端），渐变明度按路径哈希微调——同标签卡片也是两张不同的画（真实数据走 exe 图标/系统缩略图，无此问题）；大图标 ≥150% 档卡片补路径与标签行。
- **AI 设置页「运行打标」首屏被裁**：保存/测试/清除密钥与「运行打标」两个按钮合并为一行，入口全部进首屏。

### P1（约 13 项）

工具栏加「范围/类型」组标签、`control-chip-active` 改实心反白、筛选漏斗加激活计数徽章；标签胶囊接入命中高亮（表达式命中标签时高亮标签名，高亮永远落在真正匹配的字段上）；侧栏已选标签 `scrollIntoView` 自动定位；添加入库对话框补「附带 N 个文件直接入库」文案、「下次默认用它」改 checkbox（默认勾选，不勾则不写 `taglauncher.folder_import_mode`）；次要文字对比度实测 ≥5:1 不加深，改由 ≤75% 档隐藏路径行解决（`data-card-tiny`）；卡片 ≥150% 档与「最近使用」视图补「上次使用 x 前」（`formatRelativeTime`）；AI 打标进度条加粗并 300ms 过渡（进行中本有进度条，评审截图为起步空条）；视频不可预览改中性灰底 + VideoOff 图标（不再用彩色首帧误导「已出图」）；图谱连线水平扫掠收在子节点正上方走廊（不再横穿换行兄弟节点文字）、层标签改泳道（层数=泳道数，换行节点归属明确）、详情面板 X 改收起箭头「›」；命令面板选中行次级文字 72%→90% 白；对象标签编辑器拼音排序 + 已选置顶分隔 + ≥8 个标签出过滤框（与批量条下拉同阈值）；失效复核对话框危险按钮居左 `mr-auto`（与批量移除同一套肌肉记忆）、列表高度自适应（46vh 封顶）、头部补琥珀警告徽章、找回失败主按钮文案变「重新扫描」；快捷键面板「排除/取消排除」行芯片顶对齐、末组不再跨列（两列 13/12 行平衡）、说明文字改 ⓘ 注释样式。

### P2（约 27 项，全做）

命令面板删输入行 Ctrl+K 徽章（底栏并入「↑↓ 选择 · Enter 执行 · Esc 关闭」）、列表最小高度 200px；文件夹预览行压 40px 并补「共 N 项」、四种预览底栏统一「复制路径 + 打开」；音频进度条 hover 加粗 + 手柄放大 + 热区 ±8px；右键菜单缩略图两项收「缩略图 ›」二级菜单（复用文件柜子菜单机制）；批量条「已标 n/11」改迷你徽章、全标行色点叠勾、加「只看未标全」开关；标签关系编辑器左列改纵向列表 + 当前行竖条；主题 Gallery 色块加大、族名旁 3 主色点 + HEX；「安全」分组改名「备份与导出」；「清除密码」红色文字按钮挪密码行右侧；更新页收进设置卡；Mod 卡片补「已禁用」灰 pill；骨架屏按视口铺满 + 侧栏骨架条 4 种宽度循环；「移出资料库」→「移出库」（含 design test 同步）；欢迎页关闭 X 改小号无边框；disabled 主按钮全局去饱和灰（`action-button-primary:disabled` 与品牌色脱钩）；框选矩形描边 1.5px/60% + 填充 12%；状态栏删排序指示与「已选 n」（选择态计数由批量条承载）；空态「清空所有筛选」仅在有筛选激活时出现、卡片上移至视口上 1/3；0 计数标签 40% 弱化；侧栏 ≥10 个标签出迷你过滤框；已选标签芯片底色 20%→12%（黑标签不再重一圈）；列表星标移行尾常驻（跨视图「星标在右侧」语言统一）；排序「智能」tooltip 已有且与实现逐字对应（核实不改），方向切换因排序模型无方向字段不做。

### 第三轮明确不改

- 顶栏「电源」图标：实为 Info（关于）且有 tooltip，评审误判。
- 暗色标题栏太阳图标底块：常态无底色，随标题栏按钮删除自然消解。
- 黑色标签选中「黑斑」：选中行底色/色条/文字公式全标签统一，黑色是合法用户色（素墨裁定：用户数据不去色）；仅把顶部芯片底色减到 12%。
- 排序方向切换：数据模型无 asc/desc 字段，不为此扩展模型。
- 侧栏底部教程条：早已持久化关闭（`taglauncher.sidebar_hint_dismissed` + 6 次自动隐去），评审未知情。

### 验证

`tsc` 干净；逻辑测试 227/227、vitest 121/121；`node scripts/demo-screenshots.mjs` 断言 90/90、66 张截图人工复核通过（四族亮暗、深色边框、图谱、52/53 危险居左、AI 首屏、樱花画布、素墨 hairline）。巡演脚本同步：明暗切换改走 `#sidebar-theme-mode-*`、确认框定位改 `/^移出库/`。

---

## 13. 第四轮：交互深度优化 + 性能整改 + 性能矩阵（2026-09-21）

用户三问：全部修复；item/输入交互别扭，对标商业成熟设计；性能不达标（启动慢、滚动/加载卡、切主题卡）。三路取证（item 交互 / 输入链路 / 性能热点）归因闭环；底层（虚拟化、拼音索引缓存、SQL）健康，痛点集中在键盘事件归属分裂、拖拽可达性、IME 系统性缺口、三类级联放大。裁决：typeahead + Ctrl+G/I/L；建 10k/50k 性能矩阵（Mod KV 上限维持不做）。

### Batch I · item 交互（对标 Explorer / VS Code / Raycast）

- **焦点统一（roving focus）**：活动项 = 选中集末尾（无新状态）；卡片 `tabIndex={active?0:-1}`、容器成唯一 Tab 停点（活动项未挂载时 0）；方向键经 pending-focus 标记在行挂载后真实聚焦（500ms 失效兜底）；删卡片内嵌 Enter（Enter 只剩 window 一条路径，恒启动活动项，与 Space 同指一项）；卡内按钮退出 Tab 序（收藏=Ctrl+D、启动=Enter、预览=Space）。预览分支补 `onButton` 守卫（焦点在预览按钮上时 Enter/Space 让按钮原生激活）。
- **启动后视图稳定**：会话内冻结排序键（`SortKeyOverrides.lastUsedAt`）——启动只刷新时间不瞬移；快照在 loadAll/排序变更/视图域切换重拍；收藏切换用活值立即跨区；搜索打分不受影响。
- **拖拽可达性**：卡片/行本体可拖（6px 阈值，交互子元素除外）；拖动已选中项 = 拖整个选中集（Explorer 语义），幽灵显示「拖动 N 个项目」；落点批量结算（文件柜 `addItemsToCabinet`、收藏 `setFavorites`、移除区批量移除）；共享 `useItemDragStart` hook 统一卡片与行。
- **拖拽成功反馈**：收藏/入柜成功 toast（单命名、批量计数）；已在柜/已收藏给 info。
- **空白右键背景菜单**：添加文件 / 添加文件夹 / 全选 / 清空选择 / 刷新（复用 MenuItem 与 modal-surface）。
- **框选**：Ctrl+框选 = 对称差加选（Explorer Ctrl 语义）；激活阈值统一欧氏 6px。
- **typeahead**：裸打字母/汉字跳名称匹配项（600ms 缓冲、前缀优先），G/I/L 改 Ctrl+G/I/L（快捷键帮助与 USER_GUIDE 同步）。
- 标签 pill 单击也选中卡片（行内任意位置一致）。

### Batch II · 输入修复

- **IME 系统性缺口**：`useEscapeKey` 全局栈加 isComposing 守卫——中文输入按 Esc 取消拼音候选不再关掉全部 20 处浮层。
- **Esc 两级**：命令面板/对象标签编辑器/批量条过滤框/快捷键帮助——有词先清词，再按才关。
- **过滤输入 IME 守卫**：`useImeComposition`（显示文本与应用查询分离）接入侧栏标签过滤、批量条、ItemTagsEditor、TagRelationsEditor、ShortcutsHelp 五处。
- **高亮缓存**：`splitHighlightSegments` (text,query) LRU + 汉字段拼音 LRU——逐击键跨卡片零重复 pinyin 调用；`WorkspaceScopeHeader` 改直订阅 searchQuery（不再连坐 inputValue）。
- **命令面板**：对象搜索 ≥2 字符才建索引起搜（单字符不再扫全库）；首次开面板不再全量建索引。
- **搜索索引闲时预热**：loadAll 后 requestIdleCallback/200ms 兜底构建（取消/卸载可清理），首次击键零构建；防抖窗口内 `ensurePinyin()` 门控。
- **compositionend 双发守卫**：mod 监听器不再收双份。

### Batch III · 性能修复

- **切主题级联消除（最大收益）**：色位写回后不再 `TAGS_WRITTEN_EVENT` 全量重取——`ITEM_TAG_COLORS_PATCH_EVENT` 按 tag id 就地改对象内嵌 pill 色（原级联 = get_items 全表 + 磁盘对账 + 图标重取 + 全网格重渲染，1k ≈ 200-500ms）。
- **get_items 与对账解耦（Rust）**：`get_items`/`get_cabinet_items` 纯读化；新增 `reconcile_runtime`（启动后 2s 首跑、60s 周期节流、in_flight 防重入）+ `reconcile_items` 命令（wait 同步 / 调度异步）；有写入才 emit `items-reconciled`，前端监听走纯读 loadAll；手动刷新（按钮/命令面板）显式 `refresh({reconcile:true})`。行为时序变化：失效徽标由「下次读取立即自愈」改为「首跑 2s 或事件后」，窗口内启动该对象仍由 `resolve_current_path` 自愈。
- **图标链路**：loadAll 不再无条件失效图标缓存（手动刷新入口单独负责）；前端空值 TTL 10s→60s；后端 `.none` 冷却 30s→10min。
- **尺寸滑杆**：rAF 节流写 store、松手才序列化（拖一次只写一次盘）。
- **启动链路**：boot 主题与持久化主题相同时跳过第二次 applyTheme（消闪切）；pinyin-pro 拆懒加载分片（`pinyinProvider`，主包 884KB→592KB，词典 311KB 按需）；WelcomeModal 本已懒分片。

### 验证

tsc 干净；逻辑 228/228、vitest 121/121、cargo lib 148 + 集成全绿；demo:shots 断言 93/93（新增 typeahead 与 Ctrl+G/I/L）；release exe 经 `npm run tauri build -- --no-bundle` 构建（裸 `cargo build --release` 不带 custom-protocol 会起 devUrl 请求而 chrome-error，本次踩坑登记）。性能矩阵数字见《测试计划.md》§18。

### 第四轮不做

- Mod KV 上限（维持 2026-09-17 裁定）。
- Delete 键改删文件（保持「从库移除」+ 弹框兜底）。
- 启动后窗口自动隐藏（Raycast 范式，与整理工具定位不符）。
- CI 接入性能矩阵（本地手动跑：`python scripts/perf-matrix.py --counts 1000,10000,50000 --repeat 5 --baseline <旧报告>`）。

---

## 14. 第四轮空上下文评审全量落地（2026-09-21）

背景：`宣传视频/e2e-review/_评审报告汇总-第四轮.md`（66 张截图、3 名空上下文「艺术家&&产品设计师」代理，本轮无 P0）。作者裁决：全面修复；智能排序语义明确；文案优化；列表密度采用 C 建议加紧凑档（A「密度自适应满意」落选）。欢迎页赞赏区不纳入（沿用既有裁定）。

### W1 对比度与暗色层级公式（共识 #1，token 层）

- 亮面次要文字四族统一加深一档（text-2 → ~8.5:1、text-3 → ~6.7:1）；暗面统一上调（text-3 ≥4.8:1）。
- 暗色三级亮度阶拉开：surface 提亮（霜靛 #212a3a / 藤色 #2b2736 / 樱花 #2e2229 / 素墨 #2b2b2b），页面底不动。
- 暗色 tag pill 先压 12% 明度再取 30% 实底——高饱和 pill 在暗底不再发荧光。
- 藤色暗底降紫饱和（向中性紫灰靠）；樱花亮 accent 降饱和 ~17% 并向品红推 6°（#d63865→#c94578，与标签红拉开色相距离，TAGS 首位同步）；藤色/樱花暗琥珀提亮（#deb44e / #e2b851）。
- 素墨亮 chip alpha 加实（9%→14%）：卡片胶囊与侧栏彩点保持同一色彩索引。
- 全部偏离登记 `themeVariables.test.ts` TUNED 清单（8 板 × text×2 + accent×3 + warning×2，附理由）。

### W2 空态/低信息态补偿（共识 #2）

- 空态面板垂直居中（撤掉 mt-[12vh]）；空库面板放大为近全区拖放引导面（880px 宽、52vh、2px 沾主色虚线框）+ 上手三步（拖入→打标签→Ctrl K 搜索）+ 概念注脚分层（`EmptyStateCopy.footnote`）。
- 视频不可预览：16:9 舞台收缩为 clamp(220px,32vh,300px) 窄条，图标/说明对比度上调。
- 命令面板高度随内容自适应（撤 min-h）；⌘ 图标换 Zap（macOS 符号平台违和）。
- 更新页单卡片窄栏居中 + 「查看更新日志」入口；数据管理四分区统一卡片化。
- 骨架屏标签区补圆点占位（与真实行同构）。

### W3 图标与顶栏（共识 #3）

- `.icon-button` 全局色 text-tertiary→text-secondary（线性图标不再「没画完」）；顶栏右簇图标 14→17px。
- 筛选按钮激活筛选时实心主色填充 + 计数点反白；tooltip 补全（设置含 Ctrl+,）；设置/关于间加分隔。
- 「添加文件/添加文件夹」合并为「添加 +」下拉菜单（视觉重量还给搜索框）。
- 文件夹预览 Office 三件套按扩展名着色（docx=info / xlsx=success / pptx=warning，全走语义 token）。

### W4 按钮主次与危险动线（共识 #4）

- AI 页主按钮唯一化（保存配置）；运行打标成组居右全次级。
- 打标完成弹窗「查看结果」升主按钮（无结果时「关闭」仍主）。
- 移除确认：删盘按钮文案「删除本地文件（进回收站）」；「不再询问」注明只豁免出库、删盘仍逐次确认。
- 清除密码并入 action-button 同一节奏（不再裸红字悬浮）。

### W5 chips 色彩规则（共识 #5）

- 对象标签编辑器：未选一律中性灰底（色相只由圆点承担），已选 = 色底 15% + 同色描边 + ✓ 双编码。
- 批量加标签菜单：计数改浅色小字紧随标签名，行高收 32px。
- 侧栏标签选中 3px 色条、批量条危险项分隔线：核查已存在，未重复改。

### W6 智能排序语义 + 表头排序

- SortMode 扩方向变体：name-desc / added-desc；排序下拉 7 项分两组（推荐/字段），「智能」改名「智能（收藏·最近）」并全量补 hint。
- 列表表头名称列循环 升序→降序→智能，箭头方向即升降序（类型列维持二态）。

### W8 列表紧凑密度档

- `listDensity`（comfortable/compact）持久化；紧凑档行高 56px（一屏 +21%），骨架/虚拟化/框选度量同步。
- 入口在状态栏（list 视图时缩放槽位换 舒适/紧凑 分段）。
- 列表行分隔线 72%→88% border-default（亮色下不再串行）。

### W9 P2 散项

- 卡片标签行恒单行 + 右缘渐隐（全尺寸档网格等高）；侧栏顶部统计块删除（计数在导航行/分区标题已有）；03 被选文件夹改纵向列表（3 行 + 等 N 个）；图片 blur-fill 提亮降饱和（0.55→0.8/0.85sat）；右键子菜单展开态浅蓝填充；关系编辑器左栏 scrollbar-gutter 恒留；图谱层标列加宽不换行；AI 滑杆补 5/10/15 刻度 + 模型 datalist；打标进度轨道 10% 墨底常驻；Mod 禁用改描边徽章；快捷键「右键已选标签/侧栏 Alt+单击」拆双 chip。

### 核实后无需改（评审误判或已满足）

- 沉浸式标题栏：早已自绘（decorations:false + TitleBar 组件，色随 token）。
- 失效 pill：本就是打开复核对话框的按钮。
- 未收藏星标：已 opacity-40 半隐 + hover 显现。
- 侧栏标签 3px 色条、批量条危险项分隔、音频轨道 4px+44px 热区、框选填充 12%、快捷键分组图标统一 accent：核查均已满足。
- 骨架屏顶栏「半真半假」：搜索/视图/添加控件加载期本就可用，无「未就绪」错觉，不改。
- 列表常显「上次使用」列：与作者采纳的紧凑档直接冲突，recent 视图已显示，不改。
- 欢迎页 X 紫框：焦点环截图伪影（a11y 必需），非样式缺陷。

### 验证

tsc 干净；逻辑 230/230、vitest 121/121；demo:shots 断言 95/95、66 张截图。脚本同步：表头排序改三态循环、排序选项 7 项分组、AI 主按钮断言改为保存配置、设置/筛选选择器跟 tooltip 文案走、Gallery 截图补 hover 态区分。

---

## 15. 第五轮空上下文评审落地（2026-09-22）

背景：`宣传视频/e2e-review/_评审报告汇总-第五轮.md`（66 张截图、1 名空上下文「艺术家&&产品设计师」代理，本轮无 P0）。按「截图与代码对得上的缺陷改根因」执行；欢迎页赞赏区不纳入（沿用裁定）。

### 已落地

| 项 | 根因 | 落地 |
|---|---|---|
| 类型口径混用（列表「PowerShell/批处理/应用程序」vs 筛选「脚本/程序」） | `TYPE_LABELS` 七值 vs `TYPE_FILTERS` 六类两套映射 | `TYPE_LABELS` 对齐六类（程序/脚本）；`TYPE_DETAILS` + `getTypeDetail` 把技术名收进 tooltip（「脚本 · PowerShell」）；卡片/列表/命令面板共用 |
| 批量「加入标签」开关被裁 | 弹层 `min-w-190` 把过滤框与「只看未标全」挤一行 | 弹层 `min-w-248`，开关独占一行 |
| 「清除密钥」与测试连接同权 | 危险动作缺危险语义 | 改 `action-button-danger` |
| 空态「清空所有筛选」恒次级 | 与「清空搜索」同级恢复动作主次漂移 | 单独出现时升 primary；两者并存保持一次一主 |
| 排序分组名「字段」表意弱 | 组名像可点排序项 | 改「排序依据」 |
| 侧栏主题色点发现性弱 | 4 枚无字色点（tooltip 本有，截图看不出） | 色点行加「主题」微标 + hover 微放大；G3 色点保留 |
| 失效项元行缺失 | 从未启动的失效对象无「上次使用」行，高度不齐 | 元行档位内补「失效 · 可找回」 |
| 列表星标贴类型文案 | 星标被读成类型一部分 | 星标独立 28px 动作位 |
| 入库文件夹名单薄 | 纯文本与说明段混排 | 文件夹图标 + 凹槽行 |
| 框选矩形偏弱 | 1.5px/60% 描边在浅紫画布上不够 | 2px/78% 描边，填充维持 12% |
| 动效位点不完整 | 按钮/图标无过渡；遮罩无入场 | `action-button`/`icon-button` 补 fast 过渡与按压行程；遮罩底衬 120ms 淡入；图谱连线 stroke 150ms |
| 命令面板表头拥挤 | 组名与计数贴边 | gap + 计数 shrink-0 |

### 核实后不改（评审误判 / 与既有裁定冲突 / 超范围）

- **空库仍列标签**：A2 作者裁定「有标签就列出」，文案已承认「项目不在了，标签还在」。
- **欢迎页 X 焦点环**：a11y 必需，截图伪影（第四轮已裁）。
- **模态遮罩偏暗**：W3「遮罩够暗」为作者要的层次；轻/重分档不改。
- **骨架屏顶栏可点**：加载期本就可用，无「未就绪」错觉（第四轮已裁）。
- **Gallery 8 vs 色点 4**：Gallery 与色点同为 `THEME_FAMILIES` 四族；子代理误读成 8 卡。
- **色点无 tooltip**：`title`/`aria-label` 本就有家族名，截图看不出 tooltip。
- **视频失败态要海报+播放钮**：第三轮裁定不用彩色首帧误导「已出图」；demo 无片源走失败态，真片源走 `<video controls>`。
- **大图标插画降饱和**：内容图标是产品最强资产（前两轮正面评价）；改由名称/类型层级收口，不动插画饱和度。
- **找回失败加「手动定位」**：属新子系统，原则「构图可以改，子系统不新开」。

### 顺带修的测试债（1923cc9 漂移）

- `itemUtils.design.test.ts`：标签改为六类口径 + `getTypeDetail` 用例。
- `itemActionCopy.design.test.ts`：多选开菜单对齐「打开此项」（不承诺批量启动）。
- `themeVariables.test.ts`：tag-pill 描边 46% 配方；hover 不再 `translateY(-1px)`（只改描边）。

### 验证

`tsc` 干净；前端逻辑 232/232、vitest 121/121；`npm run demo:shots` 断言 95/95、66 张截图抽查通过（02 类型口径与「主题」微标、39 批量菜单开关独立成行、19 空态主按钮）。
