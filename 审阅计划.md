# TagLauncher 审阅计划

> 更新日期：2026-09-17。
> 基线：当前工作区（界面优化 W1–W5 之后的复验轮；相对已发布 `1.8.0` 另含文件夹链路与色位写回）。
> 范围：审阅计划 R01～R13 + `recolor_tags_and_cabinets` / `useTagColorSlotSync`。
> 状态：本轮已执行。结论见 §7。

[测试计划](测试计划.md)定义场景和测试门槛；[后续计划](后续计划.md)定义问题修复与交付顺序。本计划以代码无 bug、业务正确、事务一致、可维护和实现简洁为审阅标准。

## 1. 审阅范围和证据规则

- 审阅提交内的 UI、性能、CLI/TUI/MCP、视频、构建、测试及文档；共享模块同时检查 GUI 和 CLI 调用者。
- 以当前源码、完整调用链和实际测试为依据，核对新增实现与已有服务、主题令牌、事务和测试模式的复用情况。
- 现有运行总结、绿色测试和截图作为背景证据；每条通过结论标明所覆盖的行为与未覆盖环境。
- 已完成 Run 的报告保持封存。审阅结果写入新的审阅记录，并引用原报告或测试记录。
- 公开计划和审阅产物只使用应用自己的术语、色值和路径。指定私有设计源在本机读取，检查对齐产物的命名、注释、资源和提交信息脱敏。

## 2. 严重程度

| 等级 | 定义 | 处置 |
|---|---|---|
| P0 | 数据丢失或误关联、越界写入、无法升级/启动、错误架构或缺失必要程序的交付包 | 阻断候选版本验收，先复现和修复 |
| P1 | 核心交互不可用、持续卡顿、缓存/状态错误、明显资源泄漏或关键可访问性问题 | 验收前关闭并补回归 |
| P2 | 局部一致性、重复计算、可维护性和非关键文档问题 | 明确优先级与验收条件后安排 |

下表中的“优先级”是审阅顺序；实际发现问题后按影响重新定级。

## 3. 审阅检查表

| ID | 优先级 | 当前入口与观察 | 需要取得的结论 | 对应测试 |
|---|---|---|---|---|
| R01 | P0 | `src-tauri/src/services/item_service.rs`：`scan_dir_tree` 跳过部分目录、条目、元数据读取错误；预算耗尽有显式错误 | 可读范围、异常中断和“唯一候选”语义一致；不可读路径与预算耗尽的用户反馈清楚；重复根/硬链接行为确定 | SCN-01～03 |
| R02 | P0 | 同文件：`plan_signature_relocations`、`apply_signature_relocations`、`apply_reconcile`；`file_identity.rs` | 原路径/签名守卫覆盖竞争写入；身份、内容采集和提交之间的变化有处理；错误分类、回滚和跳过语义明确；GUI/CLI 无过期覆盖 | SCN-04～06、ID-04～06 |
| R03 | P0 | `db/schema.rs`、`db/migrations/v011_video_type.rs`、`v012_fts_maintenance.rs`、`db/connection.rs` | v4/v10/v11 升级到 12 后对象、标签、柜、FTS 和凭据一致；中断可恢复；未来 schema 有明确行为；正常重开幂等 | DAT-09～13、IDX-01/02 |
| R04 | P0 | `services/icon_service.rs`、`services/shell_thumbnail.rs`：全局 Shell 锁、临时文件及多种图像提取分支 | COM/GDI/HICON/HBITMAP 生命周期配对；慢处理器影响有界；同进程/跨进程读写和失败清理可靠；PNG alpha 正确；视频、音频和快捷方式的缓存发布策略清楚 | VIS-04/05 |
| R05 | P1 | `src/lib/itemVisualCache.ts`：TTL 过期删除条目；`useItemVisual.ts`、`useItems.ts` | 活跃订阅在过期、刷新、重新挂载时持续获取结果；在途结果归属正确；失败可恢复；可淘汰缓存与活跃订阅分别有可解释上界 | VIS-01～03 |
| R06 | P1 | `src/lib/search.ts`、`itemQuery.ts`、`useSearch.ts`、`useItems.ts` | 归一化、同义词、英文单次编辑和表达式编译保持查询语义与稳定排序；首次拼音输入、长输入和 50k 数据集下主线程开销有测量 | SR-01～11、测试计划 §16 |
| R07 | P1 | `useTheme.ts`、`SettingsPanel.tsx`：当前主题按 id 查询，分区隐藏时保留组件 | 同 id 主题重载时元数据/变量/变体同步；初始化超时、系统模式和即时选择竞争一致；草稿保持、忙状态和配置失败反馈正确 | TH-01～07、SET-01/02 |
| R08 | P1 | `useFocusTrap.ts`、编辑器、`TagGraphView.tsx`、`ContextMenu.tsx`、扩展确认 portal | Tab/Esc 在隐藏区块、嵌套菜单和 portal 中正确；长内容/缩放保持按钮可达；图谱尺寸更新与键盘选择完整；反选和批量操作业务一致 | UI-03～16、ACC-01 |
| R09 | P0 | `src-tauri/src/cli/mod.rs` 的 `open_db` 调用 `Database::new`；`bin/tl.rs`、`cli/mcp.rs`、`cli/tui.rs` | 已初始化库、残缺库、未来 schema、共享库锁竞争行为符合文档；只读命令的初始化副作用可追踪；批量命令错误和退出码明确 | CLI-01～03、MCP-01/02、TUI-01 |
| R10 | P0 | `scripts/prepare-cli-bin.mjs`、`build-portable.mjs`、`Cargo.toml`、`tauri.conf.json`、CI/release | GUI 主程序、sidecar 路径和架构一致；新克隆可构建；占位和陈旧 CLI 不进入候选包；Windows 路径含空格/引号时打包正确 | PKG-01/02、ENG-06～11 |
| R11 | P0 | `scripts/e2e-data-migration.py`：v10 夹具仅含部分表列；图标目录由 KnownFolder 解析 | 测试夹具对应真实版本结构；DB/WebView2/系统缓存/旧库探测均隔离；测试进程和句柄退出后才清理目标 | 测试计划 T0、T4、T6 |
| R12 | P1 | `settings_commands.rs`、`data_commands.rs`、`sync_commands.rs`、Mod 命令、`update_commands.rs` | 凭据查询、导出/云副本脱敏保持；可信扩展边界描述准确；新 IPC 权限/注册完整；更新资产缺少当前架构时行为明确 | SEC-01～06、AI-02/03、SYNC-01～09、UPD-02 |
| R13 | P2 | `scripts/run-tests.mjs`、`测试计划.md`、README/手册与 `skills/tag-launcher/SKILL.md` | 测试计数去重、错误退出完整；声明的自动覆盖都有断言；当前 schema、主题数量、便携包文件清单、CLI 语义和验证说明一致 | ENG-01～05、测试计划 §17 |

## 4. 实际执行步骤

1. **固定基线**：记录 HEAD、父提交和工作区状态。提取完整变更清单，按 R01～R13 关联到模块和调用者。
2. **核验现有约定**：检查三个既有模式——批量事务及故障回滚、锁外计划/锁内守卫回写、主题变量/共享控件。记录本次改动的复用点与偏差。
3. **先审 P0**：数据、迁移、文件系统、CLI 共享库和交付包。每个疑点先完成最小复现，发现阻断问题时保存测试夹具与失败证据。
4. **再审 P1**：缓存/搜索/主题竞态与键盘路径，同时使用对应测试场景检验推论。
5. **核验真实渲染**：按测试计划的 18 套主题、桌面尺寸和控件状态做一轮批量检查；材料包括稳定帧截图、计算样式、操作结果及性能记录。
6. **形成结果**：逐条记录问题、影响、位置、复现、最小修复方向、回归用例及验收条件。未知项保留为待核验。
7. **复审修复**：使用修复后的提交运行相关回归，之后统一执行测试计划 T2～T6。新证据关联对应问题 ID。

## 5. 审阅结果格式

每条记录采用：

```text
ID / 严重程度 / 状态（待核验、已复现、修复中、已验证、接受）
问题及实际影响
触发条件和最小步骤
当前提交与文件:行号
预期 / 实际
证据（失败日志、断言、截图、性能样本）
最小修复范围 / 回归用例 ID / 复核结果
```

“接受”包含适用边界、代价和用户决定。目录/计数/类型等源码观察与已复现缺陷分开记录。现有 `.workflow/tmp/sec-review-findings.md` 仅作为线索：其中空名称校验等结论需按当前代码核对，更新资产回退等条目需单独复验。

## 6. 退出条件

- [ ] R01～R13 均有检查记录，关键调用者已核对。
- [ ] P0/P1 已关闭或完成有证据的定级调整；待核验高影响项有明确处置。
- [ ] 测试计划中的覆盖缺口逐项落到后续任务；自动化范围与手册一致。
- [x] release 候选包以 x64 清单/哈希为准；ARM64 归 CI。安装/升级/卸载实机矩阵已永久取消。
- [ ] 完成脱敏、差异清洁度及重复实现检查。

正式审阅的执行结果由下方 §7 承载。

## 7. 本轮审阅结论（2026-09-17）

对照当前源码，不按提交行数或测试总数下结论。

| ID | 结论 | 处置 |
|---|---|---|
| R01 扫描完成性 | `scan_dir_tree` 预算耗尽有显式未完成、不接受唯一候选；权限/离线盘注入仍缺真实 I/O 夹具 | 通过现有单测；SCN-02 环境受限 |
| R02 身份重定位 | 计划后守卫与歧义拒绝已有测试；克隆盘组合仍待补受控夹具 | 通过现有 identity 测试；ID-04 待补 |
| R03 迁移 | 最新 schema **14**。v4→最新、幂等重开已有测试。旧审阅写 12 已过时 | 本轮补「未来版本拒绝打开」 |
| R04 Shell 资源 | 并发发布单测在；release 原生冒烟已取到 PNG。图标缓存目录仍是 `%LOCALAPPDATA%\com.taglauncher.app\item-icons\`，不受 datapath 沙箱约束 | 通过；隔离缺口记 P2 |
| R05 图标缓存 TTL | **已复现**：TTL 过期时 `subscribeItemVisual` 整条删除，会丢掉仍挂着的 listener | 本轮修复，补 VIS-02 |
| R06 搜索 | 设计测试覆盖长词/同义词/容错；50k 主线程测量仍用旧样本 | 逻辑通过；性能对照走 F07 |
| R07 主题 | 同 id 竞态有 `useTheme.race`；色位写回已有 hook/计划测试 | 通过；真库往返仍建议实机抽查 |
| R08 焦点/图谱 | 组件测试覆盖；release 原生冒烟已跑焦点循环与 Gallery | ACC-01 自动 + 原生通过 |
| R09 CLI 共享库 | 无库拒绝静默建库。未来 schema 过去会当最新库打开 | 本轮在 `run_pending` 拒绝更高版本 |
| R10 打包 sidecar | 脚本与 CI 仍按 x64 本机 + ARM64 CI | 本轮打 x64 候选包；ARM64 环境受限 |
| R11 迁移夹具 | v10 夹具原先只有 id/name/path/type，**v011 重建 items 会因缺列失败** | 本轮补齐 v10 表结构；断言 schema 14 |
| R12 更新资产 | **已复现**：已知架构找不到匹配后缀时盲回退第一个 .exe | 本轮改为空 URL，仅未知架构回退 |
| R13 文档/计数 | schema 12/v008、旧主题下拉、`cargo test` 重复计数均过时 | 本轮对齐文档与 `cargo test --tests` |

旧 `sec-review-findings.md`：F1 空名称校验**代码里已有**，本轮补回归；F2 本轮修复；F3/F4 维持低优先级；F5/F6 接受。

色位写回专项：单元/hook 覆盖首次进入、空目录不盖章、IPC 失败不持久化、自定义主题不改色。真库霜靛→素墨→樱花往返仍标「待实机抽查」，不视为本轮阻断。
