# 文件夹链路审阅与监视方案

> 日期：2026-09-17  
> 性质：决策记录 + 现状审阅 + 后续方案（**本次不写代码**）  
> 适用范围：TagLauncher 当前工作区（含「添加文件夹二选一」「删除可出回收站」「失效待处理」「大图标」等已落地改动）  
> 对照文档：[PROJECT_MANUAL.md](./PROJECT_MANUAL.md)、[USER_GUIDE.md](./USER_GUIDE.md)、[测试计划.md](./测试计划.md)

---

## 0. 决策摘要

| 编号 | 议题 | 决策 | 本次是否实现 |
|---|---|---|---|
| D | 导入文件夹时选「文件夹本身」或「夹内文件」 | 已落地，保持 | 已完成 |
| E | 文件夹新增文件自动入库 | **可以做，但必须由用户手动打开监视后才生效**；默认不监视 | **否**（本文只定方案） |
| F | 失效对象勾选后出库 | 已落地，保持 | 已完成 |
| C | 删除时选仅出库或连源文件进回收站 | 已落地，保持 | 已完成 |
| H | 外面删了源文件就自动出库，U 盘拔出仍标失效 | **需求自身不自洽，明确不做** | **否** |

一句话：

- 文件夹在库里首先是**一条可打开的目录对象**，不是隐式媒体库根。
- 「把夹里的文件收进来」是**一次性快照**。
- 「以后夹里新文件也进来」是**另一项能力**，必须在某个文件夹对象上**手动打开监视**。
- 「路径暂时找不到」一律走失效 + 用户勾选清理，**不靠猜测区分「人删了」和「盘拔了」**。

---

## 1. 背景：用户原话要解决什么

评论里和文件夹相关的诉求可以收成三句：

1. 导入一个目录后，希望看见**里面的文件**，而不是只看见文件夹这一条。
2. 之后往这个目录（含子目录）丢新文件，希望库里**跟着出现**。
3. 在应用里删视频时希望能删源文件；外面删了又希望库记录消失，但 U 盘拔出仍要标失效并插回可开。

第 1 句已由导入对话框覆盖。第 2 句是 E。第 3 句后半是 H。

作者当时把 2、3 理解成「把整个文件夹当库、再从应用里删掉这个文件夹」。这个理解偏了：用户要的不是「文件夹当一条启动项就可以删」，而是「目录当容器，内容当对象」。但若把每个导入的目录都默认为持续同步的库根，就会和现有「对象列表 + 标签 + 文件柜」模型打架。因此 E 必须做成**显式监视**，不能做成导入的副作用。

---

## 2. 概念地图：项目里有四种「文件夹」

审阅的第一件事是把词分开。当前代码里至少有四套语义，界面有时混用。

| 名称 | 它是什么 | 落在哪里 | 和磁盘目录的关系 |
|---|---|---|---|
| 对象类型 `folder` | 库里的一条记录，`path` 指向一个目录 | `items.type = 'folder'` | 1:1 指向该目录；双击用 `ShellExecuteW("open")` 打开 |
| 导入展开 | 遍历目录，把**文件**收成多条对象 | `expand_folder_import` | 一次性快照；**不**把目录本身入库 |
| 文件柜 | 虚拟集合，一对象可入多柜 | `cabinets` / `cabinet_items` | **没有**对应的磁盘目录 |
| 监视根（尚未存在） | 「这个目录里以后出现的文件也入库」 | 方案见 §6 | 必须挂在某个 `folder` 对象上，且默认关 |

另外还有两条**只读窥视**，不要和入库混淆：

- 快速预览列目录：`list_object_directory`，一层、最多取 5000 条，UI 再截到 48 条。
- 跨盘找回扫描：`scan_dir_tree`，按内容签名在候选盘上找**已失效文件**，不是导入，也不是监视。

批量工具条上的按钮文案「文件夹 / 从当前文件夹移出」指的是**文件柜**，不是磁盘文件夹。这是当前链路里最容易把用户和开发者同时带偏的命名。

---

## 3. 全链路审阅

下面按真实调用顺序走一遍。每段先写「现在做什么」，再写「是否合理」。

### 3.1 入口：谁能把路径送进库

| 入口 | 是否经过 `useFolderImport.requestAddPaths` | 含文件夹时是否弹二选一 |
|---|---|---|
| 顶栏「添加文件夹」 | 是 | 是 |
| 空库引导「添加文件夹」 | 是（`viewProps.onAddItems`） | 是 |
| 命令面板「添加文件夹」 | 是 | 是 |
| 外部拖放（Tauri + DOM） | 是 | 是 |
| 顶栏「添加文件」 | 是，但分类结果通常无目录 | 否 |
| CLI `tl` / MCP `add_items` | **否**，直接 `item_service::add_items` | 否，目录会变成一条 `folder` 对象 |
| Mod `api.addItems` | **否** | 否 |

**结论：** GUI 主路径已经收敛到同一进口，这是对的。CLI / MCP / Mod 故意保持「路径是什么就加什么」，不要在无 UI 的通道里弹窗。后续做监视时，这两条通道也**不得**因为加了一个目录就自动开监视。

导入方式偏好存在 `localStorage` 键 `taglauncher.folder_import_mode`，只影响对话框默认选项，不进备份、不同步、不进 CLI。对「一次性导入」足够；不能拿它当监视开关。

### 3.2 分类：`classify_import_paths`

`Path::is_dir()` 为真 → 文件夹，否则当文件。空串丢掉。路径不存在时按文件处理，交给 `add_items` 报错或落一条将来会标失效的记录。

**合理：** 分类只做文件系统事实，不做产品决策。  
**缺口：** 不存在的路径和普通文件走同一侧，对话框不会出现，用户可能莫名其妙多一条失效对象。可接受，不必为监视改这里。

### 3.3 二选一：文件夹本身 vs 夹内文件

- **文件夹本身：** `add_items(原路径)`。每个目录一条 `folder` 对象。子文件不入库。预览可以看见一层内容，但不等于库成员。
- **夹内文件：** `expand_folder_import` 后 `add_items(文件路径)`。递归收文件，**不**收中间目录，**不**收根目录本身。跳过符号链接、隐藏/系统属性、`.` / `$` 前缀，以及 `desktop.ini` / `Thumbs.db` 等。单次最多 2000 个文件，截断会 toast。

**合理：** 这正好对应用户的两种用法——「我要一个可打开的目录」和「我要里面的片子」。二者互斥，避免「文件夹 + 里面所有文件」重复占库。  
**必须写进产品说明的后果：**

1. 选「夹内文件」之后，库里**没有**可以挂监视的根。以后要监视，得再添加一次「文件夹本身」，再手动打开监视。
2. 选「文件夹本身」之后，库里**没有**子文件对象。预览能看 48 项，但不能打标签、不能按视频筛选这些子文件。
3. 展开规则和预览列举规则不一致（见 3.6）。这是有意的：预览求快求全貌，导入求可管理对象。

未知扩展名经 `detect_type` 归入 `exe`（语义是「用关联程序打开的通用文件」）。展开不会按视频/图片再滤一层。若用户把整盘文档目录按「夹内文件」加进来，会冒出大量「程序」类对象。这是导入选择问题，不是分类 bug。

### 3.4 落库与身份

`add_items` 对每个路径：锁外采身份 / 签名 / 类型，锁内按 `(volume_serial, file_id)` 去重，没有身份则按 `path` 去重。

| | 普通文件 | 目录对象 |
|---|---|---|
| NTFS 文件 ID | 有 | 通常也有（目录在 NTFS 上同样有 File ID） |
| 同盘改名/移动 | 能跟上 | 能跟上（`file_identity` 有目录改名测试） |
| 内容签名 | 有（空文件除外） | **无**（`compute_signature` 对非文件返回 `None`） |
| 跨盘移动兜底 | 可按签名扫描 | **不能**按内容找回，只能等路径重新出现或用户再拖入 |

**合理：** 目录没有稳定「内容」，不算签名是对的。  
**产品含义：** 把整个资料夹从 `D:` 剪到 `E:` 时，文件夹对象更容易变成失效，且状态栏「尝试找回」帮不上忙。监视根若建立在文件夹对象上，跨盘后必须先让这个对象恢复（再拖入或路径对上），监视才能续上。不要幻想监视自己能跨盘跟着走。

### 3.5 工作台展示

文件夹对象和文件对象走同一套卡片 / 列表 / 大图标。类型筛选有「文件夹」。大图标模式藏路径、藏标签，文件夹会变成一块目录图标 + 名称，适合「少而大的目录入口」，不适合当媒体墙——媒体墙应使用「夹内文件」导入后的视频/图片对象。

**合理。** 大图标不是为文件夹发明的，不必为文件夹单独做第三套卡片。

### 3.6 预览：看见内容 ≠ 在库里

`QuickPreview` 对 `type === "folder"`：

1. `get_object_file_info`
2. `list_object_directory`（一层，后端上限 5000，前端 `slice(0, 48)`）
3. 列表只展示名称，不能点进子目录，也不会「加入库」

预览**不**跳过隐藏文件（导入展开会跳）。同一目录在预览里可能看见 `desktop.ini`，导入「夹内文件」时却不会入库。

**合理：** 预览是资源管理器式窥视；导入是策展。  
**交互债：** 用户可能以为「预览里看见的就是库」。需要在预览文案或导入说明里写清「目录内容仅预览，不会自动变成对象」。当前文案是「目录内容（最多 48 项）」，没有这句话。

### 3.7 启动与「打开所在位置」

`launch_item` 对所有类型一律 `ShellExecuteW("open")`。文件夹会进资源管理器，这是对的。`open_in_explorer` 对目录打开自身，对文件打开父目录，也是对的。

监视**不应**改变启动语义。监视根仍然是「打开这个目录」，不是「打开这个虚拟库」。

### 3.8 删除

现在有两档：

- **仅出库：** 磁盘不动。文件夹对象消失，里面的真实文件都还在；若以前用「夹内文件」导过子文件，那些对象还在。
- **删除本地文件：** `SHFileOperationW(FO_DELETE + FOF_ALLOWUNDO)` 进回收站。目标是**文件夹对象**时，回收站里是**整棵目录树**。

**合理处：** 危险操作要用户再点一次；「下次不再确认」只作用于仅出库。  
**风险：** 文件夹对象的「删除本地文件」杀伤力和文件对象不是一个量级。确认框目前用同一句「文件或文件夹」，没有对目录加粗警告。后续做监视时更危险：一个监视根下面可能已经有几百个入库文件，再删这个目录等于同时让这批对象全部失效（H 不做的话它们会留在失效列表里）。

出库文件夹对象**不应**自动出库「曾经从它展开进去的文件」——库里没有这层父子外键。这是当前模型的诚实之处，也是监视方案必须补「根 → 子对象」关系的原因（见 §6.3）。

### 3.9 对账与失效

刷新时 `plan_reconcile` 只问 `Path::exists()`：

- 还在：清失效，必要时回填身份/签名
- 不在：按文件 ID 在同卷重定位；不行就 `is_missing = 1`

U 盘拔出、资源管理器里删除、改名到没扫到的位置、权限不足，对对账来说是同一类「此刻打不开」。状态栏「N 个失效 · 待处理」列出它们，用户可「尝试找回全部」或勾选出库。这就是 F。

**合理，且必须保持。** 这是产品特色，评论里用户明确说 U 盘这条要留。

### 3.10 文件柜

文件柜是标签之外的另一套归类，和磁盘目录无关。文件夹对象可以进柜，展开进来的文件也可以进柜。柜成员变化不会改监视，监视也不应改柜。

**不合理的只有文案：** 工具条把柜叫做「文件夹」。监视上线后，若再出现「监视文件夹」，三词撞车。建议在实现监视前把工具条改回「文件柜 / 从当前文件柜移出」。

### 3.11 三条遍历实现，规则不统一

| 遍历 | 用途 | 深度 | 上限 | 跳过规则 |
|---|---|---|---|---|
| `expand_folder_import` | 导入夹内文件 | 递归 | 2000 文件 | 隐藏/系统、垃圾名、符号链接 |
| `list_object_directory` | 预览 | 一层 | 5000 条目（UI 48） | 仅符号链接 |
| `scan_dir_tree` | 失效签名找回 | 递归 | 扫描预算 | junction、若干系统目录名 |

**审阅意见：** 三套并存可以接受，因为目的不同。但「导入」和「监视」必须共用**同一套**跳过规则与上限策略，否则用户会觉得「我手动加的时候跳过了桌面.ini，监视却又加进来」。监视应直接复用 `import_paths` 的跳过函数，而不是再写第四份。

### 3.12 文档与演示的偏差

| 材料 | 偏差 |
|---|---|
| [USER_GUIDE.md](./USER_GUIDE.md) | 仍写「不移动、不复制、不改动你的任何文件」；C 已能把源文件送进回收站。未写导入二选一、大图标、失效待处理勾选。 |
| [PROJECT_MANUAL.md](./PROJECT_MANUAL.md) | 视图偏好仍写网格/列表；失效徽标仍写成「点击即找回」，现在是先打开待处理。无导入二选一、无回收站删除。 |
| Demo `expand_folder_import` | 浏览器演示无法真遍历，只是丢掉「看起来像文件夹」的路径，和真机行为不一致。 |

这些是文档债，不是运行时错误。本报告不改用户手册正文（避免和「只出报告」混成一次大改文档）；实现监视或发版说明时应一并改。

---

## 4. 链路合理性总评

**站得住的主轴：**

TagLauncher 管的是**对象**，不是磁盘树。标签和文件柜负责归类，路径只是最近已知位置。文件夹对象是「可启动的目录入口」，预览是窥视，展开导入是快照。这条主轴和「不默认当媒体库」一致，也和评论里「现在的功能已经很好了」兼容。

**真正的结构缺口只有一处：**

库里没有「这个文件是从哪一个目录根收进来的」。因此：

- 不能对某个根做「再扫描一次」
- 不能把监视范围限制在「我收过的那棵树」
- 删除文件夹对象时，无法提示「下面还有 N 个已入库文件」
- 选「夹内文件」之后，没有自然的开关去打开 E

E 的正确补法不是「导入即监视」，而是：**先有文件夹对象，再在这个对象上动手打开监视；监视负责把该根下的新文件 `add_items`。** 这正是本次对 E 的决策。

**建议在实现监视前先修的低成本一致性问题（非本报告范围，记在这里）：**

1. 批量工具条「文件夹」改回「文件柜」。
2. 删除本地文件时，若选中含 `type=folder`，确认框单独警告「将把整个目录移到回收站」。
3. 文件夹预览补一句「仅预览，不会自动加入库」。
4. 发版时改 USER_GUIDE / PROJECT_MANUAL 里已过时的「绝不改动本地文件」和失效入口描述。

---

## 5. E 方案：监视必须手动打开才生效

### 5.1 产品原则

1. **默认不监视。** 添加文件夹、展开夹内文件、拖放、启动，都不会开始盯盘。
2. **开关挂在文件夹对象上。** 只有 `type=folder` 且未失效的对象可以打开「监视此文件夹」。
3. **全局总闸可选。** 设置里增加「允许文件夹监视」，用于一次暂停所有监视而不丢掉各对象上的开关。总闸默认**开**（否则用户打开对象开关却没反应）；对象开关默认**关**。
4. **导入对话框不自动打钩。** 可以在「添加文件夹本身」下方加一行说明：「若要自动收入以后的新文件，请添加后在右键菜单打开监视。」不要第三种导入模式，避免和二选一抢决策。
5. **监视只做加法。** 新文件入库；文件从磁盘消失仍走现有对账（标失效）和 F（用户勾选出库）。不在监视回调里自动 `remove_items`。
6. **和文件柜无关。** 监视根不是柜，柜不是监视根。

这样，用户原话里的「对某些父级文件夹进行监控」变成：先把父级加成文件夹对象，再手动打开监视。没有打开的，永远不会静默涨库。

### 5.2 用户能看见的行为

**打开监视之后：**

- 该目录及其子目录里**新出现的文件**（创建、从别处移入）按导入展开的同一套跳过规则入库。
- 已在库中的文件（身份或路径命中）不会重复建行。
- 新出现的子文件夹**不会**自动变成文件夹对象，以免监视根复制出一棵对象树。
- 该文件夹对象失效（U 盘拔出、目录被删）时，这条监视暂停；对象恢复后再继续。
- 应用没开时漏掉的文件：下次启动或该根恢复时，做一次**有上限的补扫**（复用 `expand_folder_import` 的规则与 2000 预算，或按根单独预算），把仍不在库里的文件补进来。补扫不是全盘找回。

**关闭监视之后：**

- 立刻停止该根的变更通知。
- 已经入库的对象全部保留。
- 再往目录里丢文件，库不再涨。

**关掉全局总闸：**

- 所有根停止通知和补扫。
- 各对象上的「监视此文件夹」勾选保留，总闸重新打开后恢复。

### 5.3 数据怎么放

不要只写 `localStorage`。监视属于库的一部分，要能进备份 / 导入导出 / CLI 只读查询。

推荐单独表，避免给所有 `items` 行加无意义列：

```text
watch_roots
  item_id     INTEGER PRIMARY KEY REFERENCES items(id) ON DELETE CASCADE
  enabled     INTEGER NOT NULL DEFAULT 1
  recursive   INTEGER NOT NULL DEFAULT 1
  last_scan_at DATETIME
```

- 行存在且 `enabled=1` 且全局总闸开且对应 item 为 `folder` 且 `is_missing=0` → 真正监视。
- 删除文件夹对象（仅出库）时级联丢掉监视根，这是对的：根都没了。
- `recursive` 先固定为 1，列先留着，避免用户评论里「父级下面的子文件夹」还要再迁一次 schema。

全局总闸：`app_meta.folder_watch_master = "0" | "1"`，缺省当 `"1"`。

对象开关的 UI：右键「监视此文件夹」勾选态；仅 `type=folder` 显示。设置页「数据管理」放总闸和一句说明。

### 5.4 和现有模块怎么接

| 步骤 | 复用 | 不要做 |
|---|---|---|
| 发现新路径 | `ReadDirectoryChangesW`（或等价）+ 防抖 | 自己实现一套递归枚举器 |
| 过滤 | `import_paths` 的跳过名 / 隐藏 / 符号链接 | 第四份 skip 列表 |
| 入库 | 现有 `add_items` | 监视线程里直接 INSERT |
| 应用未运行时补漏 | 对每个监视根调用与 `expand_folder_import` 相同的收集，再 `add_items` | 启动时全盘扫描 |
| 文件消失 | 现有 `plan_reconcile` + F | 监视回调里出库 |
| 锁 | 通知在锁外，写库走现有两段式 | 在 DB 锁里等文件系统 |

CLI / MCP 若要开监视，另加显式命令（例如 `tl watch add <id>`），默认 `add_items` 一个目录仍然只是加对象。

### 5.5 明确不做的监视行为

- 导入「夹内文件」时偷偷建一个隐藏根并打开监视。
- 监视到新目录就再挂一条文件夹对象再递归监视（对象爆炸）。
- 监视到删除就出库（这是 H，已否决）。
- 把监视根画成侧边栏目录树，替代标签/文件柜。
- 对网盘根默认打开监视（抖动会把库刷爆；用户要开必须自己开，总闸和对象开关仍在）。

### 5.6 风险与验收（供以后实现时用）

| 风险 | 处理 |
|---|---|
| 一次丢进上万文件 | 沿用 2000/批 + toast，其余下次补扫或用户再开一次「立即扫描此根」 |
| U 盘抖动 | 根失效即停；恢复后补扫，不在断开期间狂报错 |
| 应用占用目录导致删不掉 | 通知用异步，不长期持有根上的列举句柄超出需要 |
| 用户忘了自己开过监视 | 状态栏或设置里显示「正在监视 N 个文件夹」 |
| 与「夹内文件」重复 | `add_items` 身份去重已够；补扫也走同一去重 |

验收最小集：

1. 新加的文件夹对象默认不监视，往里丢文件库不变。
2. 打开监视后丢一个 `mp4`，库里多一条视频，且去重。
3. 关掉监视后再丢，库不再增加。
4. 拔掉该盘所在卷，对象标失效，监视停；插回后对象恢复，监视恢复。
5. 总闸关闭后，即使对象开关仍勾选，也不入库。
6. 仅出库该文件夹对象后，监视根行消失。

---

## 6. H 为什么不做

### 6.1 用户想要的两条规则

- 我在资源管理器里删了源文件 → 库记录也消失，不要标失效。
- U 盘拔出 → 继续标失效，插回去还能打开。这是特色，要留。

两条分开看都像合理。放进同一个系统就不成立。

### 6.2 操作系统给不出「意图」

对账能看见的只有：这个路径现在 `exists()` 是不是真。

| 真实世界发生的事 | `exists()` | 用户心里的分类 |
|---|---|---|
| U 盘拔出 | false | 离线，要留 |
| 在资源管理器删了 | false | 删了，要出库 |
| 移动到尚未扫描的位置 | false | 常常仍想找回 |
| 网盘掉线 / BitLocker 未解锁 | false | 离线 |
| 权限或占用导致本次打不开 | false | 不是删除 |
| U 盘还插着，用户清掉盘上的片子 | false | 有人当「删了」，有人当「盘还在、文件暂缺」 |

「盘符还在、文件不在」并不能推出「用户要出库」：U 盘挂着、人删了里面的电影，按 H 的字面规则会出库并丢掉标签；这和「U 盘特色要留」直接打架。

「盘符不在」也不能推出「一定是离线」：用户可能删了整个盘符上的资料夹，或盘符被资源管理器移走。

所以 H 要求产品在**同一信号**上猜意图。猜错的代价是丢掉标签、文件柜、收藏——比多一条失效贵得多。

### 6.3 和已经落地的能力重复且冲突

| 已有能力 | 用户已经能做的事 |
|---|---|
| C 删除本地文件 | **明确说**「连源文件一起丢掉」，库记录一并去掉 |
| C 仅出库 | 明确说「磁盘不动」 |
| F 失效待处理 | 人已经不在了的条目，勾选后出库 |
| 对账 + 签名找回 | 暂时找不到先留着，能对上再挂回去 |

H 想做的是：外面删了就自动走「出库」。那 F 的待处理列表里就只剩 U 盘离线。可「外面删了」和「U 盘离线」无法可靠分开，于是 F 要么误杀离线盘上的对象，要么漏掉真删除。C 已经覆盖「我就是要连文件一起删」。外面删除若真要清理，用户打开失效待处理勾选即可——这正是「手动」，和 E 的精神一致。

### 6.4 和身份模型的承诺相反

项目手册写明：删除 / 离线标失效但**保留归类**；跨盘用签名兜底。若外面一删就出库：

- 用户把文件剪到另一台盘、或先删再从回收站还原到别的路径，签名找回没有对象可写回去。
- 「失效可感知恢复」不再是完整策略，而是残缺策略。

评论里被夸的，恰恰是这套「先标失效、插回还能开」。H 要拆掉它的一半，又留着另一半，只靠猜。

### 6.5 结论

H 不是「难做」，是**规则写不自洽**。正确的产品分配是：

- 暂时找不到 → 失效（含 U 盘）
- 我确认不要了 → C 或 F，由用户动手
- 不要让程序在背后把「找不到」翻译成「用户删了」

因此 H **不做**，也不在监视方案里夹带「侦测到 DELETE 就出库」。

---

## 7. 和评论需求的对照（收口）

| 评论诉求 | 现状 / 决策 |
|---|---|
| 视频缩略图要画面不是图标 | 已有系统首帧；抽不到才回退图标（A，已迭代） |
| 视频能筛选、别算程序 | 已有 `video` 类型与筛选（B，已完成） |
| 删除可选是否删源文件 | C 已落地，进回收站 |
| 导入文件夹要能列出里面的文件 | D 已落地，「夹内文件」一次性展开 |
| 父级目录新增文件自动入库 | E：仅当该文件夹对象**手动打开监视**；默认不做 |
| 失效批量清理 | F 已落地，pending 式勾选 |
| 大图标、名称在下、路径可藏 | G 已落地 |
| 外面删了不要标失效，U 盘拔出仍要 | H：不自洽，不做；用 F 手动清 |

---

## 8. 以后若做 E，建议顺序

本次只出方案。若立项，建议按这个顺序，避免先写通知再发现模型不够：

1. 工具条文案「文件柜」与删除目录警告（小、减误解）。
2. `watch_roots` 表 + 全局总闸 + 右键开关（先能持久化，不必先挂钩子）。
3. 启动补扫（复用 `expand_folder_import`），无通知也能「打开监视后扫一次」。
4. Windows 目录通知 + 防抖 + `add_items`。
5. 状态栏「正在监视 N 个文件夹」。
6. 更新 USER_GUIDE / PROJECT_MANUAL / 测试计划（监视、删除回收站、导入二选一、失效待处理、大图标）。

第 3 步单独就可交付「手动打开监视 = 立刻收一次新文件，之后每次启动再补」；第 4 步才是真正的热监视。若资源紧，可以先做 1–3，仍然满足「手动设置才生效」。

---

## 9. 本文不包含的内容

- 不修改任何功能代码。
- 不实现 `watch_roots`、不改 schema。
- 不实现 H 的任何启发式（盘符在/不在、父目录在/不在）。
- 不把文件柜改造成磁盘树。
- 不在本次更新 USER_GUIDE / PROJECT_MANUAL 正文（偏差已登记在 §3.12、§4）。
