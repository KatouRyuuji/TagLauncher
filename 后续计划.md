# TagLauncher 后续计划

> 更新日期：2026-09-17。
> 基线：界面优化后的当前工作区（`ddd4897` + 本轮复验修复）；应用 1.8.0，schema **14**。
> 状态：W0 入口已落地并开始复验；W1 按审阅复现修复（图标 TTL、更新包回退、未来 schema、v10 夹具）；W2/W3 随 `verify:candidate` 与 x64 打包推进。界面优化闸门记录仍有效，不代替本轮证据。

实施采用 KISS：复用现有 React、Zustand、Rust 服务、SQLite 事务和测试框架；先依据测量确认瓶颈，再选择最小改动。测试场景以 [测试计划](测试计划.md)为唯一用例目录，审查检查点见 [审阅计划](审阅计划.md)。

## 1. 当前起点

- 基线包含性能优化、主题和控件对齐、设置分区、系统图标、视频及 CLI/TUI/MCP，变更覆盖 120 个文件。
- 已留存前端、Rust、浏览器、原生和性能证据，详见测试计划 §17。安装 / 升级 / 卸载实机矩阵已永久取消，不作为缺口。
- 可运行的 debug GUI 位于 `src-tauri/target/debug/tag-launcher.exe`；安装包、便携包、ARM64、外部 API 联调以新一轮验证结果为准。
- 性能参考值包括 10k 复杂热查询约 23 ms、5k 批量标签约 206 ms；这些数值保留相同夹具和构建类型后用于复验。

## 2. 执行顺序

| 阶段 | 任务 | 进入条件 | 离开条件 |
|---|---|---|---|
| W0 建立可复验基线 | F01、F02 | 当前提交和工作区已记录 | 正确夹具、隔离目录、命令与证据清单齐备；形成逐项审阅结论 |
| W1 修复已确认问题 | F03～F06 | F02 给出复现与严重程度 | 必要修复有回归；未复现项有覆盖证据或进一步调查任务 |
| W2 统一回归与度量 | F07 | P0/P1 修复完成 | 当前候选提交通过全量测试、原生/视觉和性能矩阵 |
| W3 候选版本准备 | F08、F09 | F07 完成 | x64 包和 ARM64 构建/运行结果完整，版本与文档同步，可交付具体候选包 |

F03～F06 的实现内容由复现结果决定。确认没有缺陷的检查点记录证据后关闭，避免制造无效改动。

## 3. 任务清单

### F01 — 测试夹具、隔离和证据入口（P0，进行中）

- **范围**：`scripts/e2e-data-migration.py`、`scripts/run-tests.mjs`、测试夹具与必要 npm 入口。
- **依赖**：无。
- **工作**：核对 v10 迁移夹具完整性；使用独立 Windows 测试用户或 VM 隔离 KnownFolder、旧库探测、WebView2 和数据库；从临时原生/视觉/性能脚本提取可复跑工具，参数化路径、端口、输出目录和数据规模。
- **证据**：每次运行记录提交、二进制 SHA256、工具版本、夹具规模、原始命令、退出码、测试计数及截图。保留当前测试框架，按二进制与用例名去重 Rust 统计。
- **验收**：全新检出可复跑；测试前后真实用户目录无测试写入；失败有非零退出码；收集结果时没有被重复计算或隐含跳过的通过数。

### F02 — 完整提交审阅与缺口归档（P0，完成）

- **范围**：审阅计划 R01～R13，包含所有业务、测试、构建和文档改动。
- **依赖**：基线核对可立即开始；故障复现使用 F01 提供的隔离夹具。
- **工作**：检查数据不变式、事务边界、异步生命周期、兼容性、性能和视觉状态；复核本机旧审阅线索；按严重程度登记真实问题。
- **验收**：每个检查点都有“通过证据 / 已复现问题 / 待核验”的明确结论；问题具有文件定位、触发步骤和测试用例；审阅结论不依赖提交总行数或测试总数。

### F03 — 数据、扫描与索引正确性（P0，进行中）

- **范围**：`services/item_service.rs`、`file_identity.rs`、`db/schema.rs`、迁移链及身份测试。
- **依赖**：F02 R01～R03。
- **工作**：复现读取中断、重复根、身份与签名竞争窗口、重定位守卫、v11→v12 和未来版本场景；按结果修正扫描完成性及回写策略。
- **验收**：SCN-01～06、IDX-01/02、DAT-09～13 有证据；高影响竞争测试中没有错误重定位、归类丢失或部分事务提交；正常重开行为幂等。

### F04 — 图标队列与系统资源生命周期（P1，进行中）

- **范围**：`itemVisualCache.ts`、`useItemVisual.ts`、`icon_service.rs`、`shell_thumbnail.rs`。
- **依赖**：F02 R04/R05。
- **工作**：核验活跃订阅 TTL、同一输出跨进程访问、失败重试、Shell 慢处理器、快捷方式与音频缓存发布；测量长时间滚动的资源趋势。
- **验收**：VIS-01～05 通过；图像完整、透明通道正确，错误后可恢复；并发/缓存边界可解释；重复滚动没有无界队列、失联订阅或持续句柄增长。

### F05 — 搜索、主题与交互状态（P1，进行中）

- **范围**：查询算法和 Hook、主题 Hook、设置、图谱、焦点、批量选择、菜单/编辑器；**新增**：`useTagColorSlotSync` + `recolor_tags_and_cabinets`（切官方主题按色位写回 `tags.color` / `cabinets.color`）、`WorkspaceScopeHeader`、`ItemGrid` 少结果旁路、`preview/` 四主体、`ThemeFamilyGallery`。
- **依赖**：F02 R06～R08。
- **工作**：给优化查询算法建立基准/参考实现差分测试，覆盖长词、同义词更新和首输；核验同 id 主题更新、配置加载失败、嵌套焦点和提交期间行为；按状态补充图像对齐测试。**色位写回专项**：真库（非 demo）下霜靛→素墨→樱花往返后 `SELECT color FROM tags` 按色位还原；自定义 / Mod 主题下不改色；tags 未加载时启动切主题不清空 `taglauncher.color_slots`；IPC 失败不持久化；手选色后再切家族按旧板最近位重算。
- **验收**：SR、SET、ACC 和关键 UI 场景完整；结果 id/排序一致；草稿、主题预览和实际生效状态同步；本机指定设计源保持只读，公开产物保持脱敏。

### F06 — CLI/MCP/TUI 与 GUI 共享数据（P0/P1，进行中）

- **范围**：`src-tauri/src/bin/tl.rs`、`src-tauri/src/cli/`、共享数据库/服务，以及应用使用说明。
- **依赖**：F02 R09/R12；复用 F01 的隔离子进程工具。
- **工作**：实际运行 CLI 全命令与异常参数；MCP stdin/stdout/stderr、握手、通知、EOF、未知方法和工具错误联调；核验 GUI/CLI 并发与 TUI 退出恢复终端。
- **验收**：CLI-01～03、MCP-01/02、TUI-01 通过；数据库行为与文档一致；机器输出可解析、退出码准确；数据目录与未来 schema 处理明确。

### F07 — 当前候选提交统一验收（P0，待执行）

- **范围**：测试计划 T2～T5、§12、§16。
- **依赖**：F03～F06 的 P0/P1 问题关闭；相关修复提交已确定。
- **工作**：同一提交全量测试和构建；覆盖所有主题/菜单/编辑器状态、原生外部文件拖入、内部拖拽、剪贴板、媒体、系统对话框和扩展；收集性能原始样本。
- **验收**：所有必需场景通过，环境受限项逐项说明；10k/50k 数据集按既定性能目标评估；超过目标的结果产生具体问题和测量证据。
- **实施选择**：只有测量确认首输或主线程阻塞为瓶颈，才评估分块索引或 Worker；只有分析证明入口依赖可延迟，才进一步拆分 bundle。

### F08 — x64 与 ARM64 候选产物（P0，待执行）

- **范围**：构建脚本、CI/release、sidecar、NSIS 和便携 zip。
- **依赖**：F07 通过；F02 R10/R11 结论已落实。
- **工作**：在新检出目录构建 x64；先准备真实 tl，再运行 `npm run pack`；ARM64 使用现有 CI。核对 GUI/CLI 架构、版本、文件清单和哈希。
- **明确不做**：安装 / 升级 / 卸载 / 便携覆盖的实机矩阵。作者裁定现在不要、以后也不要。
- **验收**：PKG-01 与 T6（打包 + 清单/哈希）通过；真实 sidecar 位于正确目录；当前架构缺包时更新流程有明确反馈。

### F09 — 文档和交付收口（P1，待执行）

- **范围**：README、PROJECT_MANUAL、MAINTENANCE、USER_GUIDE、THEME_GUIDE、MOD_GUIDE、应用集成说明及本组三份计划。
- **依赖**：F07 完成后更新技术事实，F08 完成后更新产物说明。
- **工作**：按当前实现校对 schema 14、4 个内置配色家族 × 亮/暗、CLI/MCP 语义、双程序便携包和真实验证范围；保留单一事实来源，使用引用减少重复。界面优化文档债已写入 USER_GUIDE / PROJECT_MANUAL / THEME_GUIDE；`scripts/verify-layout.mjs` 改走 Gallery。
- **验收**：文字只描述实际行为及目的；日期/版本/产物/验证状态一致；发布资料完成脱敏。提交和推送、打 tag、发布分别按照用户当次指令执行。

## 4. 完成与更新方式

- [x] W0：F01 入口落地（`verify:candidate` / `test:native` / 完整 v10 夹具）；F02 结论见审阅计划 §7。
- [x] W1：TTL 订阅、更新包回退、未来 schema、空名称回归已有自动证据（`.tmp-test/evidence/2026-09-17T07-14-06-263Z`）。
- [x] W2：`verify:candidate` 全绿；`demo:shots` 80/80；release 原生冒烟通过（见 `native-result.json`）。未改搜索/bundle。
- [x] W3：x64 NSIS + 便携 zip 已产出（见 `.tmp-test/evidence/pack-1.8.0-x64.json`）。ARM64 归 CI。文档已对齐 schema 14 与界面债。未打 tag、未推送。

每项任务更新“待执行 / 进行中 / 待复核 / 完成 / 环境受限”及证据引用。源码、夹具、构建或设计源变化时，只重新打开受影响的检查点；修改当前候选提交后，整合验收记录绑定新 SHA。

本轮仅创建或更新计划文档。既有已封存执行记录保持其证据身份，新一轮审阅、测试及修复采用新的执行记录。
