# 打包发给 MiniMax H3

把 **本目录整包**（`视频素材/`）打成 zip 发给操作员。根目录就是本文件夹。

1. 打开 `h3/约束.md`
2. 按 `h3/镜次表.md` 逐镜提交
3. 每一镜提示词在 `h3/prompts/`；图片路径写在该 txt 的 `【提交参数】` 里，相对本目录根

| 类 | 位置 |
|---|---|
| 优化脚本 | `01-优化分镜脚本.md` |
| 功能特性卡片 | `功能特性卡片/` |
| 软件截图 | `软件截图/` |
| 提示词 + 约束 + 素材 | `h3/` + 上述图片目录 |
