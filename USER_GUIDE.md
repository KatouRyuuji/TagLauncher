# TagLauncher 使用手册

> 适用版本：v1.5.0 · 面向最终用户 · 开发者请见 [PROJECT_MANUAL.md](./PROJECT_MANUAL.md) 与 [TUTORIAL.md](./TUTORIAL.md)

TagLauncher 用「标签 + 文件柜 + 收藏 + 搜索」管理你的本地文件、文件夹、程序、脚本、图片和音频——不移动、不复制、不改动你的任何文件，只做轻量的索引与启动。

---

## 1. 安装与首次启动

1. 从 [GitHub Releases](https://github.com/KatouRyuuji/TagLauncher/releases) 下载对应架构的安装包：
   - 常规电脑选 `TagLauncher_x.y.z_x64-setup.exe`；
   - Windows on ARM 设备（如骁龙本）选 `..._arm64-setup.exe`。
2. 运行安装包，语言可选中文/英文；桌面快捷方式在功能选择页勾选。
3. 首次启动会显示欢迎页；应用数据默认存放在安装目录旁的 `Save/` 文件夹（不写注册表、不放 AppData，整个目录拷走即迁移）。

> 系统要求：Windows 10 1803+ / Windows 11，需要 WebView2 Runtime（安装包会自动引导安装）。

## 2. 导入与管理对象

- **导入**：把文件/文件夹直接拖进主窗口，或点击顶栏「添加」按钮选择。支持批量。
- **启动**：双击对象（或卡片上的「启动」按钮）用系统关联程序打开；单击只是选中。
- **右键菜单**：打开、快速预览、打开所在文件夹、复制路径、设置缩略图、收藏、管理标签、加入文件柜、移除等。
- **移除**：只删除 TagLauncher 里的管理记录，**不会删除你的本地文件**。
- **框选与批量**：在空白处按住拖动可框选多个对象；单击替换、Ctrl 加选、Shift 范围选择。底部工具条支持批量打标、入柜、复制路径、移除。

### 对象追踪（重命名 / 移动后不丢）

- 同一磁盘内重命名、移动文件，TagLauncher 通过 NTFS 文件 ID 自动追踪，标签归类不丢失。
- **跨盘符移动**后，通过内容签名自动找回（首次刷新时后台扫描）。
- 文件被删除或磁盘离线时，对象标记为「失效」但保留归类，文件恢复后自动复原。

### NAS / 网络路径 / 软链接

- 支持添加 UNC 路径（`\\server\share\...`）与映射网络盘（如 `Z:\`）上的对象；网络文件系统不支持文件 ID 时自动回退按路径管理。
- WebDAV 挂载盘（如坚果云挂载、rclone mount）上的对象按普通磁盘对象管理。
- 符号链接/目录联接按目标语义处理（启动的是链接指向的目标）。

## 3. 标签系统

- 侧栏「标签」页新建、编辑、删除标签；把标签**拖到对象上**即打标。
- 标签名全局唯一：与已有标签重名时保存会提示「已存在同名标签」，改名后再保存即可。
- 多选标签 = 交集筛选（同时拥有全部所选标签的对象）。
- **层级标签（DAG）**：在「关系」编辑器里给标签设置父子关系（支持多父）；筛选父标签时自动并入所有后代标签的对象。例如「游戏 ⊃ 2D游戏」，选「游戏」也会显示打了「2D游戏」的对象。
- **图谱视图**：侧栏「图谱」入口以图形化方式探索标签关系——单击节点查看关联对象，双击直接按该标签筛选。

## 4. 文件柜与收藏

- **文件柜**：像抽屉一样的自定义集合，一个对象可属于多个柜；拖拽对象到柜上即归档。柜名全局唯一，重名保存会提示「已存在同名文件柜」。
- **收藏**：拖到侧栏收藏夹或右键收藏；收藏对象在列表中置顶。
- **最近使用**：侧栏「最近使用」只显示启动过的对象。
- 标签筛选、文件柜筛选、收藏筛选、最近使用四者互斥，切换时自动清理。

## 5. 搜索、排序与类型

顶栏搜索框支持三种模式（全部 / 名称 / 标签），并内建：

- 中文**拼音**与**拼音首字母**匹配（如输入 `yx` 命中「游戏」）；
- 英文轻度容错（3 字符以上）；
- **同义词**扩展（词库文件 `synonyms.json` 可自行编辑，重启生效）；
- 表达式语法：

| 语法 | 含义 | 示例 |
|---|---|---|
| 空格 / `\|\|` | 或 | `游戏 工具` |
| `&&` | 与 | `游戏&&2d` |
| `!!` | 排除 | `游戏 !!卡牌` |
| `()` | 分组 | `(游戏\|\|工具)&&!!旧` |
| `@` | 严格匹配（关闭拼音/容错/同义词） | `@忍者` |

筛选栏可按类型（文件夹 / 图片 / 音频 / 程序 / 脚本）收窄结果；顶栏「排序」支持智能、名称、最近使用、添加时间、类型。网格/列表、搜索模式、排序与类型筛选会记住，下次启动仍在。

## 5.1 键盘与命令面板

| 快捷键 | 作用 |
|---|---|
| `/` 或 `Ctrl+F` | 聚焦搜索 |
| `Ctrl+K` | 命令面板（搜命令或直接启动对象；未输入时只显示常用命令） |
| `↑` `↓` / `←` `→` | 移动选中项（网格上下按列跳转） |
| `Home` / `End` | 跳到当前结果首项 / 末项 |
| `PageUp` / `PageDown` | 翻页选择 |
| `Shift` + 方向键 / Home / End | 范围多选 |
| `Enter` | 启动已选中项 |
| `Space` | 快速预览（预览中也可方向键 / Home / End 切换） |
| `Ctrl+A` | 全选当前结果 |
| `Ctrl+C` | 复制选中路径（多项换行） |
| `Ctrl+D` | 收藏 / 取消收藏（有未收藏则只补收藏） |
| `Delete` | 从应用移除（不删本地文件；不用 Backspace） |
| `Shift+F10` / 菜单键 | 打开选中项右键菜单 |
| `G` / `L` | 网格 / 列表 |
| `Ctrl+,` | 设置 |
| `?` | 快捷键一览 |
| `Esc` | 关闭最上层浮层；批量工具条先关下拉；或清空搜索 / 取消选择 |

命令面板里 Tab 可预览高亮对象。右键菜单也可用方向键操作，并提供「快速预览」和「复制路径」。中文输入法组字时，Enter / 字母键不会误启动或误过滤。空状态在「库里有对象但当前筛选为空」时会提示一键清空筛选。

## 6. AI 自动打标

「设置 → AI 自动打标」填写兼容 Anthropic 协议的 API 地址、密钥与模型名（三项必填，亦可用第三方兼容网关）：

- 「为未打标对象打标」/「为全部对象打标」：批量智能打标，带进度与取消；
- 「新对象自动打标」：导入新对象时后台静默打标；
- 可限制每个对象的标签数量、是否允许 AI 创建新标签、附加打标偏好（如「偏向按用途分类」）。

密钥只保存在你本机的数据库中，界面不回显明文；**导出数据与云端备份会自动剔除密钥**。

## 7. 数据管理（本机）

「设置 → 数据管理」：

- **切换数据目录**：把 `Save/`（数据库、备份）重定向到任意目录（如 D 盘、同步盘）；
- **一键备份**：快照当前数据库到 `Save/Backups/`；
- **导出**：生成可分享的 `.db` 副本（自动剔除 AI 密钥等敏感配置）；
- **导入**：用别的 TagLauncher 数据库覆盖当前数据（导入前自动备份当前库，可回退）。

## 8. 云同步（WebDAV）

「设置 → 云同步」把数据备份到你自己的云端，支持任意 WebDAV 服务：群晖/威联通 NAS、Nextcloud、坚果云、InfiniCloud 等。

1. 填写服务器地址（如 `https://dav.jianguoyun.com/dav` 或 `http://192.168.1.10:5005`）、账号与密码（坚果云等需用「应用密码/授权码」）、远端目录（默认 `TagLauncher`）；
2. 「测试连接」验证配置并自动创建远端目录；
3. 「立即备份到云端」上传当前数据快照；
4. 「查看云端备份」列出远端所有备份，可选任意一份**恢复**（恢复前自动备份本机当前数据，可回退；完成后应用自动重启）；
5. 开启「自动云备份」后，每次启动若距上次备份超过 24 小时，会后台自动备份。

安全说明：

- 云端副本**自动剔除 AI 密钥与云同步凭据**——即使云盘被他人访问也不泄露密钥；
- 恢复时本机的 AI/云同步配置自动保留，不会被云端数据覆盖；
- 远端只保留最近 10 份备份，更早的自动清理；
- 使用 `http://`（非加密）地址仅建议在可信局域网内（如家庭 NAS）。

## 9. 软件更新

「设置 → 软件更新」显示当前版本，点「检查更新」从 GitHub Releases 获取最新版本与更新说明，「下载更新」直达当前架构的安装包；下载后直接运行安装包覆盖安装即可，数据不受影响。

应用启动时也会后台自动检查（每 24 小时至多一次，同一个新版本只提醒一次），发现新版会弹出提示。

## 10. 主题与 Mod

- **主题**：「设置 → 主题外观」切换内置/自定义/Mod 主题，支持导入导出 JSON 主题文件与主题变体；自定义主题放在 `Plugins_Theme/` 目录。
- **Mod**：「设置 → 扩展」管理 Mod（启用/禁用/导入/导出/卸载）；Mod 放在 `Plugins_Mods/` 目录，支持 CSS 美化、JS 功能扩展与主题包三类。
- Mod 属**可信扩展**（非安全沙箱），请只安装来源可信的 Mod。
- 制作主题/Mod 请参考仓库内 `ExampleTheme/` 与 `ExampleMod/` 示例及开发手册。

## 11. 数据迁移到新电脑

任选其一：

1. **云同步**：旧机「立即备份到云端」→ 新机装好后配置同一 WebDAV → 「查看云端备份」→ 恢复（注意：AI 密钥不随云端走，需在新机重填）；
2. **导出/导入**：旧机「导出数据」得到 `.db` 文件 → 拷到新机 → 「导入数据」；
3. **整目录拷贝**：直接把旧机安装目录旁的 `Save/` 文件夹整个拷到新机同位置（含 AI 密钥等全部配置）。

## 12. 常见问题

| 问题 | 处理 |
|---|---|
| 对象显示「失效」 | 文件被删除/移动到未扫描位置/磁盘离线。恢复文件或重新拖入即可，归类保留 |
| 快捷键不生效 | 焦点在输入框时字母键用于输入；设置/命令面板/Mod 弹层打开时工作台快捷键会让路。先 Esc 关掉浮层，或用 Ctrl+K / Ctrl+F |
| 云端备份失败 | 检查地址/账号/密码；坚果云需用应用密码；NAS 确认 WebDAV 服务已开启 |
| 检查更新失败 | GitHub API 有访问频率限制，稍后再试；或直接访问 Releases 页面 |
| AI 打标无反应 | 确认地址/密钥/模型三项都已填写并「测试连接」通过 |
| 想彻底重置 | 关闭应用后删除 `Save/` 目录（或先备份），重启即全新状态 |

数据文件位置速查：

```text
<安装目录>/Save/taglauncher.db     主数据库
<安装目录>/Save/Backups/           本机备份（含导入/恢复前的自动安全备份）
<安装目录>/datapath.json           数据目录重定向指针（自定义过才有）
<安装目录>/synonyms.json           搜索同义词词库（可编辑）
<安装目录>/Plugins_Theme/          自定义主题
<安装目录>/Plugins_Mods/           Mod
```
