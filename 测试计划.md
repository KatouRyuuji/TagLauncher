# TagLauncher 全功能测试计划

> 更新日期：2026-09-17。应用版本：1.8.0；数据库 schema：14。
> 复验基线：界面优化后的当前工作区（原 `d396f39` 证据仅作历史对照）。
> 状态：复验入口已仓库化（`npm run verify:candidate`）。自动层由该命令重跑；原生/安装包见 §17。

本文件负责用例与执行门槛；[审阅计划](审阅计划.md)负责审查证据和问题定级；[后续计划](后续计划.md)负责依赖、优先级及实施顺序。

---

## 0. 测试策略与图例

TagLauncher 是 Tauri（Rust 后端 + React 前端）Windows 桌面应用。测试分为后端、前端纯逻辑、组件、契约、端到端和人工核验，按环境及故障注入能力选择执行层。

**测试类型**
| 记号 | 含义 | 落地位置 |
|---|---|---|
| **A-BE** | 后端自动（Rust 集成/单元测试，真实临时库） | `src-tauri/tests/*.rs` + 各模块 `#[cfg(test)]` |
| **A-FE** | 前端纯逻辑自动测试（esbuild + Node） | `src/lib/*.test.ts` |
| **A-UI** | 前端组件与 Hook 自动测试（Vitest + jsdom） | `src/**/*.{spec,test}.tsx`、`src/**/*.spec.ts` |
| **E2E** | 浏览器或原生 WebView2 场景测试 | `scripts/demo-screenshots.mjs`、待固化的原生测试入口 |
| **A-CT** | 契约自动（前后端命令映射静态核对） | `src/lib/contract.test.ts` |
| **S** | 半自动（脚本辅助 + 人工确认，如构建产物） | 见用例 |
| **M** | 手动（GUI 交互、视觉、mod/主题实机、外部 API） | §12 手动清单 |

**优先级**：P0 冒烟必过（发版阻断）· P1 核心功能 · P2 一般 · P3 边缘/增强

**通过标准**：当前提交构建成功；全部适用自动测试通过；P0/P1 场景有结果和证据；发布相关以打包产物的架构、清单和哈希为准。作者裁定：**不做、以后也不做**安装 / 升级 / 卸载实机矩阵。

**覆盖口径**：表内“自动测试”列表示检查入口。测试文件存在只证明入口存在，具体覆盖由命名用例与断言确认。“待补”表示尚需新增可执行场景。执行状态使用“待运行 / 通过 / 失败 / 环境受限 / 待补测试”；通过记录包含提交、命令、退出码、断言结果和证据路径。

---

## 1. 对象管理

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| OBJ-01 | 单个导入 | A-BE | P0 | add_items([真实文件]) → 返回 1 条 Item，字段(name/path/type)正确 | tests/objects.rs |
| OBJ-02 | 批量导入 | A-BE | P0 | add_items(N 条) → 全部入库，count=N | tests/objects.rs |
| OBJ-03 | 单项失败隔离 | A-BE | P1 | 批量混入空白路径 → 合法项入库，失败项携带原因；其他 I/O 故障另按测试矩阵复验 | src-tauri/tests/objects.rs |
| OBJ-04 | 重复导入去重 | A-BE | P1 | 同一文件二次 add → 命中既有记录、不产生重复行 | tests/identity.rs |
| OBJ-05 | 删除单个/批量 | A-BE | P0 | remove_items([ids]) → 记录删除，**本地文件不动** | tests/objects.rs |
| OBJ-06 | 大批量删除分块 | A-BE | P2 | remove_items(>500 ids) → IN 分块执行、全部删除、不触 SQLite 变量上限 | tests/objects.rs |
| OBJ-07 | 收藏置顶 | A-BE | P1 | toggle_favorite → is_favorite 翻转；get_items 收藏项排前 | tests/objects.rs |
| OBJ-08 | 类型识别 | A-BE | P1 | 覆盖 folder/image/audio/video/exe/bat/ps1；未知扩展按关联程序打开的通用文件处理 | src-tauri/tests/objects.rs |
| OBJ-09 | get_items_by_ids 排序 | A-BE | P2 | 乱序 id 列表 → 返回按默认序(收藏/最近/名称)，与 SQL ORDER BY 等价 | tests/objects.rs |
| OBJ-10 | 启动对象 | M | P1 | 双击各类型对象 → 用关联程序打开（见 SEC-01 注入防护） | §12 |
| OBJ-11 | 打开所在文件夹 | M | P2 | 右键→资源管理器定位 | §12 |
| OBJ-12 | 缩略图设置/更换/清除 | M | P2 | 设置自定义图标→显示；清除→回退默认 | §12 |

### 1.1 对象身份（NTFS 文件 ID）

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| ID-01 | 文件 ID 去重 | A-BE | P0 | 同一物理文件（卷序列号+文件ID）二次 add → 命中既有、更新位置 | tests/identity.rs |
| ID-02 | 同盘移动/重命名追踪 | A-BE | P1 | 移动文件后对账 → 按 file_id 重定位、path 更新、不失效 | tests/identity.rs |
| ID-03 | 失效标记 | A-BE | P1 | 删除源文件后 reconcile → is_missing=1；文件恢复后清除 | tests/identity.rs |
| ID-04 | 克隆盘身份冲突 | S | P1 | 构造同卷号/文件 ID、不同内容且原路径仍在的场景，验证记录独立、归类保留 | 待补受控夹具；src-tauri/src/services/item_service.rs |
| ID-05 | 同文件编辑后移动 | S | P1 | 文件编辑、同盘改名后再次导入 → 合并正确对象、刷新签名、保留标签 | 待补组合回归；src-tauri/tests/identity.rs |
| ID-06 | 唯一候选复核与回写 | A-BE | P0 | 读取失效快照→锁外计划→守卫回写；重复计划影响行数为 0 | src-tauri/tests/identity.rs |
| ID-07 | 歧义候选 | A-BE | P0 | 两个不同路径具有同签名 → 排除自动回写 | item_service.rs 单元测试 |
| ID-08 | 无身份路径回退 | A-BE/S | P1 | 无身份记录按路径去重；另用实际非 NTFS/网络盘验证返回和提示 | item_service.rs 单测 + 待运行实机场景 |

---

## 2. 标签系统（图状 DAG）

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| TAG-01 | 标签 CRUD | A-BE | P0 | add/update/remove_tag → 增改删生效；删除级联清关联 | tests/tags.rs |
| TAG-02 | set_item_tags 顺序保持 | A-BE | P1 | 设置多标签 → position 按给定序，DELETE+INSERT 原子 | tests/tags.rs |
| TAG-03 | set_many 批量原子 | A-BE | P1 | 多对象批量设标签 → 整批一事务，全成或全败 | tests/tags.rs |
| TAG-04 | 多标签交集筛选 | A-BE/A-FE | P0 | 选多标签 → AND 语义，仅含全部标签的对象 | tests/search.rs / search.design.test.ts |
| TAG-05 | DAG 多父继承 | A-BE | P1 | 一标签设多个父 → tag_relations 记录，构成有向图 | tests/tags.rs |
| TAG-06 | 环检测拒绝 | A-BE | P0 | add_tag_relation 造自环/成环 → 后端拒绝返回错误 | tests/tags.rs |
| TAG-07 | 后代展开 | A-BE | P1 | WITH RECURSIVE 展开某标签全部后代 | tests/tags.rs |
| TAG-08 | 选父并入后代对象 | A-BE/A-FE | P1 | 筛选父标签 → 结果并入打了其所有后代标签的对象 | tests/search.rs / tagGraph.design.test.ts |
| TAG-09 | 后代映射/分层 | A-FE | P2 | buildDescendantsMap/computeLayers 正确；有环输入不崩不死循环 | tagGraph.design.test.ts |
| TAG-10 | 大小写复用一致 | A-FE | P2 | AI 与 Quick Create 两路新建标签均大小写不敏感复用（不造 Dev/dev 并存） | itemUtils/契约层 |
| TAG-11 | 拖拽打标 | M | P1 | 拖标签到卡片 → 只打目标标签本身 | §12 |
| TAG-12 | 关系编辑器弹窗 | M | P1 | 侧栏「关系」→ 弹窗管理父子、环冲突即时提示（**含 portal 布局修复回归**） | §12 |
| TAG-13 | 图谱视图 | E2E/M | P1 | 点击节点展示详情，双击筛选；右侧分页、窄窗详情、连线位置正确 | 浏览器巡演；图谱专项待固化 |
| TAG-14 | 侧栏 ⊂⊃ 标注 | M | P3 | 标签卡显示父数/子数 | §12 |

---

## 3. 文件柜与收藏

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| CAB-01 | 文件柜 CRUD | A-BE | P1 | 增改删柜；删柜不删对象 | tests/cabinets.rs |
| CAB-02 | 归档/批量 | A-BE | P1 | add/remove_items_to_cabinet 批量 | tests/cabinets.rs |
| CAB-03 | 筛选状态切换 | A-UI/E2E | P1 | 标签、文件柜、收藏、最近使用切换后状态和可见对象一致 | src/stores/appStore.spec.ts；浏览器巡演 |
| CAB-04 | 切柜不闪旧内容 | M | P2 | A→B 快切 → 不闪现上一柜（cabinetItemsOwner 回归） | §12 |

---

## 4. 搜索系统

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| SR-01 | 前端三模式与后端检索 | A-FE/A-BE | P0 | GUI 全部/名称/标签分别验证；CLI/MCP 的数据库检索按其现有语义独立验证 | src/lib/search.design.test.ts；src-tauri/tests/search.rs |
| SR-02 | 前缀匹配 | A-FE | P1 | 输入前缀 → 命中 | search.design.test.ts |
| SR-03 | 拼音/首字母 | A-FE | P1 | 中文对象用拼音/首字母搜到 | search.design.test.ts |
| SR-04 | 低容错编辑距离 | A-FE | P2 | 英文轻微拼错仍命中（含空格名 / CJK 混合名 / 扩展名；查询 <5 时首字母一致） | search.design.test.ts |
| SR-05 | 同义词扩展 | A-FE | P2 | 同义词库命中 | search.design.test.ts |
| SR-06 | 表达式语法 | A-FE | P1 | `&&`/`\|\|`/`!!`/括号/`@`严格 组合正确 | search.design.test.ts |
| SR-07 | 层级筛选（前端） | A-FE | P1 | filterItemsByTags 的 expand 闭包并入后代 | search.design.test.ts |
| SR-08 | 层级筛选（后端） | A-BE | P1 | search_items EXISTS per-group 层级展开 | tests/search.rs |
| SR-09 | 特殊符号回退 | A-BE | P2 | Windows 路径符号触发 LIKE 回退，不报 FTS 错 | tests/search.rs |
| SR-10 | 收藏置顶排序 | A-FE | P2 | 结果内收藏项排前 | search.design.test.ts |
| SR-11 | 搜索防抖与取消 | A-UI | P1 | 输入值立即更新，150 ms 后查询生效；清空/卸载撤销待执行输入 | src/hooks/useSearch.spec.tsx |

---

## 5. 视图与交互

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| UI-01 | 网格/列表切换 | M | P1 | 切视图保留选中/滚动合理 | §12 |
| UI-02 | 大列表虚拟化 | M | P1 | 万级对象滚动流畅，仅渲染可见+overscan | §12 |
| UI-03 | 框选（含虚拟化外） | M | P1 | 拖框选中；拖到边缘 autoScroll 累积选中视口外项；「全选N」兜底 | §12 |
| UI-04 | 批量工具条 | M | P1 | 选中后工具条出现，批量打标/归档/复制路径/删除；Esc 先关下拉 | §12 |
| UI-05 | 右键菜单 | M | P1 | 各项动作正确（删除走确认流） | §12 |
| UI-06 | 拖拽导入 | M | P0 | 从资源管理器拖文件入库（双通道去重） | §12 |
| UI-07 | 欢迎页 | E2E/M | P2 | 首启、版本号、隐藏偏好与再次打开正确；二维码和外链人工核验 | 浏览器巡演；§12 |
| UI-08 | 删除确认统一 | M | P1 | 右键/拖拽/批量三入口均走确认弹窗（「本次跳过」） | §12 |
| UI-09 | 类型筛选与排序 | A-FE | P1 | 脚本合并 bat+ps1；smart 收藏置顶；有搜索词不重排 | itemQuery.test.ts / appStore.spec.ts |
| UI-10 | 最近使用互斥 | A-FE | P1 | showRecent 清空标签/柜/收藏 | appStore.spec.ts |
| UI-11 | 命令面板/空格预览/快捷键 | M | P1 | Ctrl+K 搜命令与对象；空格预览；/ 聚焦搜索；网格方向键按列；Home/End/翻页；IME 不误触发 | itemQuery.test.ts / §12 |
| UI-12 | 复制路径 | M | P2 | 右键单项；Ctrl+C / 批量工具条复制当前可见选中（多项换行） | itemQuery.test.ts / §12 |
| UI-13 | Toast 通知 | M | P3 | 各操作 toast 文案正确、长文换行 | §12 |
| UI-14 | 点选与范围选择 | A-FE/M | P1 | 单击替换、Ctrl 加选、Shift 范围；框选后单击不误触 | itemQuery.test.ts / §12 |
| UI-15 | 右键菜单键盘 | M | P1 | ↑↓/Home/End；子菜单 ←→；Shift+F10 打开选中项菜单 | itemQuery.test.ts / §12 |
| UI-16 | 弹层与 Mod 层级 | A-FE/M | P1 | 空格预览不盖 Mod 模态；快捷键在 overlay/IME 下让路；P 键不穿透设置 | themeVariables.test.ts / workspaceChrome.test.ts / §12 |

---

## 6. 主题系统

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| TH-01 | 三来源统一 | A-BE/M | P1 | 内置/自定义 JSON/mod 主题统一加载 | tests/extensions.rs + §12 |
| TH-02 | 主题切换 | M | P1 | 切换即时生效、CSS 变量全量替换无残留 | §12 |
| TH-03 | FOUC 门控 | M | P2 | 启动不闪白/不闪旧主题 | §12 |
| TH-04 | 外部主题 CSS 和资源处理 | A-UI/S | P1 | 经真实主题应用路径核验 import/url、相对字体、asset 解析与失败反馈 | src/lib/theme.ts；扩展服务测试；待补 CSS 专项自动回归 |
| TH-05 | 内置 UUID 与来源冲突 | A-FE/A-UI/S | P1 | 内置 UUID 与家族注册一致；自定义/Mod 同 id 冲突行为有断言 | src/lib/themeVariables.test.ts、src/hooks/useTheme.race.test.tsx；冲突组合待补 |
| TH-06 | 示例主题 SkyCloud | M | P1 | 加载 100% 兼容、云朵动画、**关系/图谱弹层布局正常（portal 回归）** | §12 |
| TH-07 | 双造型主题 | E2E/M | P1 | 4 个内置家族 × 亮暗共 8 套；验证圆角、边框、阴影、选中、焦点、禁用态 | 主题契约测试 + §12；逐状态截图待复验 |

---

## 7. Mod 扩展系统

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| MOD-01 | mod 发现与 id 校验 | A-BE | P1 | discover_mods 载入期拒绝非法 id（中文/空格/超长） | tests/extensions.rs |
| MOD-02 | 权限三态 | A-BE/A-FE | P1 | 未注册拒绝/已注册 null 不受限/Set 按声明 | 契约层 + §12 |
| MOD-03 | mod_id 命令校验 | A-BE | P1 | kv/record/file 命令 ensure_valid_mod_id | tests/extensions.rs |
| MOD-04 | net.fetch SSRF | A-BE | P0 | 内网/环回/保留地址拦截、公网放行、重定向每跳复检 | tests/ai_and_net.rs |
| MOD-05 | mod KV/record/file 存取 | A-BE | P2 | 读写删各自命名空间正确 | tests/extensions.rs |
| MOD-06 | getTagRelations 权限 | A-BE/M | P2 | tags:read 权限下可读标签关系 | §12 |
| MOD-07 | 三型 mod 生命周期 | M | P1 | 启用确认→注入→面板/工具栏/插槽注册→禁用 purge 清理 | §12 |
| MOD-08 | createScope 身份绑定 | M | P2 | 执行期拒绝冒用他人 id | §12 |
| MOD-09 | 列表行插槽 | M | P3 | ItemRow header/actions/footer 三插槽 | §12 |
| MOD-10 | 示例 Mod 兼容 | M | P1 | 对象预览示例 mod 100% 兼容、API 版本 3.2.0 | §12 |

---

## 8. AI 自动打标

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| AI-01 | 配置三项必填 | A-BE | P1 | is_configured 要求 base_url+api_key+model 均非空 | tests/ai_and_net.rs |
| AI-02 | 密钥不下发前端 | A-BE | P0 | ai_get_config 返回 api_key="" + has_api_key=true | tests/ai_and_net.rs |
| AI-03 | 留空不改密钥 | A-BE | P1 | ai_set_config 密钥留空 → 保留原值 | tests/ai_and_net.rs |
| AI-04 | 清除密钥 | A-BE | P2 | ai_clear_api_key → 密钥清空、is_configured=false | tests/ai_and_net.rs |
| AI-05 | 端点归一化 | A-BE | P1 | build_endpoint 补 /v1/messages 三情形 | ai_commands.rs 单测 |
| AI-06 | 响应解析容错 | A-BE | P1 | extract_text 优先 Anthropic content（跳过 thinking）、content 字符串、OpenAI choices（含分段数组）；thinking 耗尽 max_tokens / HTTP 200 错误体 / SSE / UTF-8 BOM 均有明确处理 | ai_commands.rs 单测 |
| AI-07 | 标签数组解析 | A-BE | P1 | parse_tag_list：JSON/代码围栏/逗号与全角逗号回退；中文按字符截断 | ai_commands.rs 单测 |
| AI-08 | base_url https 强制 | A-BE | P2 | 非本机 http 拦截；[::1] 回环放行 | ai_commands.rs 单测 |
| AI-09 | 重定向不转发密钥 | A-BE | P2 | redirects(0)，3xx 显式报错不跟随 | ai_commands.rs 单测 |
| AI-10 | 为全部/未打标 | M | P1 | 触发→并发池(=3)→进度弹窗可取消 | §12 |
| AI-11 | 新对象自动打标 | M | P2 | 开关开+配置全 → 导入后台静默打标；缺 model 不空转 | §12 |
| AI-12 | 快照合并防覆盖 | M | P2 | 打标运行中手动改标签 → 不被开始时快照覆盖（getItem 重取回归） | §12 |
| AI-13 | 测试连接 | M | P1 | 填配置→测试→成功回显/失败提示；测失败密钥状态一致 | §12 |

---

## 9. 数据管理与存储

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| DAT-01 | 一键备份 | A-BE | P1 | backup_data → Save/Backups/ 页级一致快照，**保留密钥** | tests/data_mgmt.rs |
| DAT-02 | 导出脱敏 | A-BE | P0 | 副本剔除 ai.*/sync.* 并 VACUUM，二进制扫描确认明文不可检出 | src-tauri/tests/data_mgmt.rs |
| DAT-03 | 导出自我覆盖防御 | A-BE | P2 | 导出目标=实库自身 → 拒绝 | tests/data_mgmt.rs |
| DAT-04 | 导入版本校验 | A-BE | P1 | 来源版本>当前 → 拒绝导入 | tests/data_mgmt.rs |
| DAT-05 | 导入失败回滚 | A-BE/S | P0 | 服务层覆盖/回滚通过；命令层中途故障注入验证自动回滚和重启 | src-tauri/tests/data_mgmt.rs；命令故障夹具待补 |
| DAT-06 | Backup 超时不死锁 | A-BE | P1 | 目标被占用 → 15s 超时报错释放锁（非无限重试） | tests/data_mgmt.rs |
| DAT-07 | 自定义数据目录 | A-BE/M | P1 | datapath.json 重定向读写；仅重定向 Save/ | tests/data_mgmt.rs + §12 |
| DAT-08 | 恢复默认数据目录 | A-BE/E2E | P1 | 清除 datapath.json 重定向，目标为 %LOCALAPPDATA%/TagLauncher/Save；对象与备份完整 | path_service.rs；原生数据目录矩阵 |
| DAT-09 | 全新库与升级链 | A-BE | P0 | 全新库及 v4/v10/v11 升级到 latest_schema_version()（当前 14）；对象、关系、凭据保留 | src-tauri/tests/migrations.rs；v012_fts_maintenance.rs；v014_watch_roots.rs；tests/watch.rs |
| DAT-10 | 迁移事务原子 | A-BE | P1 | up+record+version 单事务，失败整体回滚 | tests/migrations.rs |
| DAT-11 | 破坏性迁移外键保留 | A-BE | P0 | v004/v005 重建 items → item_tags/cabinet_items 关联存活 | tests/migrations.rs |
| DAT-12 | 初始化与迁移幂等 | A-BE | P0 | schema 12 重开/重复迁移，数据与版本稳定；完整路径索引复用 | src-tauri/tests/migrations.rs；schema.rs 单测 |
| DAT-13 | 老库 WAL 安全迁移 | A-BE | P2 | migrate_legacy_db VACUUM INTO 一致快照（只读源回退） | tests/data_mgmt.rs |
| DAT-14 | 导入后重启恢复 | M | P1 | import→restart→数据完整可用 | §12 |

---

## 9.1 云同步与在线更新

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| SYNC-01 | 密码不下发前端 | A-BE | P0 | sync_get_config 返回 password="" + hasPassword | 命令层设计（同 AI-02 模式） |
| SYNC-02 | 云端副本剔除敏感键 | A-BE | P0 | strip_cloud_secrets → ai.%/sync.% 清空 + VACUUM 无明文残留 | tests/sync_update.rs |
| SYNC-03 | 恢复保留本机凭据 | A-BE | P0 | read_local_secrets → overwrite → reapply：数据回云端态、凭据留本机值 | tests/sync_update.rs |
| SYNC-04 | 本机凭据优先 | A-BE | P1 | 未剔除副本内嵌旧凭据 → 恢复后仍为本机现值 | tests/sync_update.rs |
| SYNC-05 | multistatus 解析 | A-BE | P1 | Apache/Nextcloud/无前缀三形态 + href 百分号解码 | sync_commands.rs 单测 |
| SYNC-06 | URL 构造与编码 | A-BE | P1 | 路径段百分号编码（中文目录）、remote_dir 规范化拒 ../ | sync_commands.rs 单测 |
| SYNC-07 | 旧份清理选择 | A-BE | P2 | select_stale_backups 保留最新 N 份 | sync_commands.rs 单测 |
| SYNC-08 | 恢复文件名防御 | A-BE | P2 | 含 / \ 或非 .db 的名字 → 拒绝 | 命令层校验 |
| SYNC-09 | 真实 WebDAV 往返 | M | P1 | 配置真实端点：测试连接→备份→列表→恢复→重启数据完整 | §12 |
| SYNC-10 | 自动云备份 | M | P2 | 开启开关 → 启动 15s 后自动备份（>24h 才触发）；失败有提示 | §12 |
| UPD-01 | Release 解析与版本比较 | A-BE | P1 | tag 带/不带 v 前缀、latest>current 才提示 | update_commands.rs 单测 |
| UPD-02 | 更新包架构选择 | A-BE/S | P0 | 覆盖匹配、只提供另一架构、只有签名文件和空资产；跨架构误选为阻断项 | update_commands.rs、src-tauri/tests/sync_update.rs；仅另一架构场景待补 |
| UPD-03 | 真实形态 payload | A-BE | P2 | 含 .sig 干扰资产的完整 Release JSON → 命中正确安装包 | tests/sync_update.rs |
| UPD-04 | 无资产回退 | A-BE | P2 | assets 空 → installer_url 空、保留 release_url | update_commands.rs 单测 |
| UPD-05 | 设置页检查更新 | M | P1 | 点击→显示最新版/说明/下载按钮跳浏览器 | §12 |
| UPD-06 | 启动自动检查节流 | M | P2 | 24h 内不重复请求；同版本只 toast 一次 | §12 |

---

## 10. 工程与构建

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| ENG-01 | tsc 类型检查 | A-FE | P0 | npx tsc --noEmit 0 错误 | run-tests.mjs |
| ENG-02 | cargo test | A-BE | P0 | 全部后端测试绿灯 | run-tests.mjs |
| ENG-03 | 前端逻辑测试 | A-FE | P0 | 全部 *.test.ts 通过 | run-tests.mjs |
| ENG-04 | 命令契约一致 | A-CT | P0 | 前端 invoke ↔ 后端注册 双向零漂移 | contract.test.ts |
| ENG-05 | vite 构建 | S | P0 | npm run build 成功产出 dist | 手动/CI |
| ENG-06 | 应用版本三处一致 | A-CT | P0 | package.json/Cargo.toml/tauri.conf.json 版本一致；schema 版本单独验证 | src/lib/contract.test.ts |
| ENG-07 | setup.bat 环境配置 | M | P2 | 干净环境一键装 Node/Rust/BuildTools/WebView2 | §12 |
| ENG-08 | build.bat / dev.bat | M | P1 | 构建/开发模式正常；bat 全英文+CRLF | §12 |
| ENG-09 | ARM64 构建及运行 | S | P1 | CI 构建 ARM64 GUI、tl 与安装/便携产物；目标架构运行有证据 | .github/workflows/release.yml；产物验收待执行 |
| ENG-10 | Release workflow | S | P1 | tag 触发双架构草稿 Release；手动触发产 artifact | GitHub Actions |
| ENG-11 | CI 测试 workflow | S | P1 | push/PR 自动 npm run test:all + 构建校验（ci.yml） | GitHub Actions |

---

## 11. 安全（横切）

| ID | 用例 | 类型 | 优先级 | 步骤 → 预期 | 自动测试 |
|---|---|---|---|---|---|
| SEC-01 | 命令注入防护 | A-BE/M | P0 | 对象名含 `x&calc` 启动 → ShellExecuteW 不经 shell、不执行 calc | 代码审计 + §12 |
| SEC-02 | SSRF 拦截 | A-BE | P0 | net_fetch/is_disallowed_ip 拦内网、放公网、IPv4-mapped 防绕过 | tests/ai_and_net.rs |
| SEC-03 | AI 密钥最小暴露 | A-BE | P0 | 不下发前端、导出剔除；请求鉴权为 x-api-key + Bearer，重定向不跟随 | tests/ai_and_net.rs + data_mgmt.rs |
| SEC-04 | 路径穿越防护 | A-BE | P1 | mod 文件命令 resolve_mod_file_path canonicalize 防 ../ 逃逸 | tests/extensions.rs |
| SEC-05 | mod 信任模型 | M | P2 | permissions 为可信扩展的能力声明（非沙箱）；启用有确认 | §12 + 文档 |
| SEC-06 | CSP/devCsp | M | P1 | 生产 CSP 严格、dev HMR 正常（**需实机验证**） | §12 |

---

## 12. 手动测试执行清单

本节在独立 Windows 测试用户或虚拟机内执行。数据库、WebView2、系统应用缓存、旧库探测目录、导出和备份均落入测试边界；执行前检查实际解析路径。开发行为使用开发模式，发布验收使用通过 Tauri 构建入口生成的嵌入资源应用。

**P0 冒烟（发版必过）**
- [ ] 拖拽导入各类型对象（UI-06）
- [ ] 启动对象含特殊字符名不触发注入（SEC-01）
- [ ] CSP 下主界面渲染 + ExampleMod 加载 + 缩略图/主题正常（SEC-06 / MOD-10 / TH-06）
- [ ] dev.bat HMR 热更正常（SEC-06）

**P1 核心交互**
- [ ] 网格/列表切换、虚拟化滚动（UI-01/02）
- [ ] 框选（含视口外 autoScroll + 全选兜底）（UI-03）
- [ ] 点选 / Ctrl / Shift 范围与右键保持多选（UI-14/15）
- [ ] 命令面板、空格预览、快捷键与 IME（UI-11）
- [ ] 示例 Mod 预览不被内置空格预览盖住，设置打开时 P 不穿透（UI-16 / MOD-10）
- [ ] 删除三入口统一确认（UI-08）
- [ ] 拖拽打标、关系编辑器弹窗**布局正常**、图谱视图（TAG-11/12/13）
- [ ] 主题切换、SkyCloud 示例主题弹层布局（TH-02/06 — portal 修复回归）
- [ ] AI 全库打标 + 进度取消 + 测试连接（AI-10/13）
- [ ] 三型 mod 生命周期（MOD-07）
- [ ] 导入后重启数据完整（DAT-14）
- [ ] build.bat / dev.bat 构建运行（ENG-08）
- [ ] 云同步真实 WebDAV 往返：测试连接→备份→列表→恢复（SYNC-09）
- [ ] 设置页检查更新展示与下载跳转（UPD-05）

**P2 及回归专项**
- [ ] 8 套主题按各自造型核验圆角/表面/边框/阴影；实际模式、预览和持久化一致（TH-07）
- [ ] 切柜不闪旧内容（CAB-04）
- [ ] AI 打标运行中改标签不被覆盖（AI-12）
- [ ] 自动打标缺 model 不空转（AI-11）
- [ ] Ctrl+C 多选复制路径、Ctrl+D 收藏（UI-12）

---

## 13. 执行顺序与命令

以下命令从项目根目录运行。依赖安装遵循现有 Node/Rust 工具链。

| 阶段 | 入口 | 通过证据 |
|---|---|---|
| T0 基线与隔离 | 核对 HEAD、工作区、三处应用版本、schema 注册表；验证测试路径 | 提交 SHA、版本、操作系统/WebView2/工具版本和隔离目录 |
| T1 依赖与快速验证 | `npm ci`；`node scripts/ensure-cli-placeholder.mjs`；`npm test` | 前端逻辑和组件用例全部通过 |
| T2 当前基线全量 | `npm run test:all`；`npm run build` | 各步骤退出码为 0，失败、跳过和环境受限逐项列明 |
| T3 主题与交互 | `npm run demo:shots -- --out .tmp-test/acceptance-d396f39/ui --port 5299` | 所有断言通过、无页面错误、截图目检；测试输出目录专用 |
| T4 原生 debug | `npm run tauri -- build --debug --no-bundle` | 主程序为 tag-launcher.exe，脱离开发服务器正常运行；执行 §12 |
| T5 性能 | `cargo test --manifest-path src-tauri/Cargo.toml --test performance -- --nocapture`；性能矩阵见 §16 | 相同夹具的功能等价、指标和资源占用记录 |
| T6 release 交付 | `npm run pack`；核对应提交的 x64 安装包与便携 zip 清单/哈希；ARM64 归 CI | GUI 与 tl 都在包内。**不跑**安装 / 升级 / 卸载 / 覆盖实机矩阵（永久不做） |

分层定位命令：

```powershell
npx tsc --noEmit
npm run test:unit
cargo test --manifest-path src-tauri/Cargo.toml --test identity --test migrations --test cabinets
cargo test --manifest-path src-tauri/Cargo.toml --lib services::shell_thumbnail -- --nocapture
```

真实视频测试先准备测试视频，将绝对路径放入进程级 `TL_THUMB_SMOKE`，再执行：

```powershell
cargo test --manifest-path src-tauri/Cargo.toml --lib extracts_thumbnail_from_real_video -- --ignored --nocapture
```

测试材料存在、确实发生提取且 PNG 可解码，才将其记录为通过。结束后清除该进程环境变量。

原生数据目录矩阵入口为 `python scripts/e2e-data-migration.py <测试二进制绝对路径>`。执行前完成 [后续计划](后续计划.md) 的 F01：当前脚本的 v10 夹具只有部分列，且仅设置 LOCALAPPDATA；需要验证升级夹具结构及 Windows KnownFolder、WebView2 和旧库探测的隔离。

**结果计数**：`npm test` 包含纯逻辑和 Vitest；类型检查归 `test:all` 或 `tsc`。当前全量运行器先执行 `cargo test --lib`，随后 `cargo test` 会再次执行库单测；统计按测试二进制与完整用例名去重。覆盖率只采用实际采集的分支/行覆盖数据。

---

## 14. 回归测试触发规则

| 改动区域 | 必跑 |
|---|---|
| 任何后端 service/db/commands | `cargo test` 全绿 + 相关 tests/*.rs |
| 迁移文件 | tests/migrations.rs 升级链 + 外键保留 + 幂等 |
| 搜索/标签图/工作台查询 | search.design.test.ts + tagGraph.design.test.ts + itemQuery.test.ts + workspaceChrome.test.ts |
| 新增/改名 Tauri 命令 | contract.test.ts（防前后端漂移） |
| 前端任意改动 | npx tsc + npm run test |
| 主题/mod 相关 | tests/extensions.rs + §12 实机项 |
| 发版前 | `npm run test:all` 全绿 + §12 P0/P1 手动清单 |

---

## 15. 本次提交专项用例

| ID | 场景与预期 | 类型/优先级 | 当前入口与补齐要求 |
|---|---|---|---|
| SCN-01 | 预算不足返回未完成，零回写；预算恰好覆盖范围时正确处理 | A-BE / P0 | item_service.rs 的 partial_scan_does_not_accept_a_single_candidate |
| SCN-02 | 枚举中断、权限拒绝、离线盘、文件改写；结果标明范围和完成性，保持对象归属 | S / P0 | scan_dir_tree；错误注入/真实 I/O 场景待补 |
| SCN-03 | 重复根、硬链接、相同大小不同签名、完全同签名多文件 | A-BE/S / P1 | 歧义已有单测；根重叠和身份去重场景待补 |
| SCN-04 | 计划后数据库路径、签名、失效状态变化或行被删除，旧计划零覆盖 | A-BE / P0 | identity.rs 的 stale_signature_plan_preserves_newer_database_state |
| SCN-05 | 候选在扫描→计划、计划→提交阶段被改写/替换/删除 | A-BE/S / P0 | signature_plan_rejects_changed_and_deleted_candidates 覆盖计划前变化；计划后变化待补 |
| SCN-06 | 文件柜对账只涉及本柜成员，卷句柄复用且释放 | A-BE / P1 | cabinets.rs；file_identity.rs |
| IDX-01 | v11→v12 修复缺项全文索引；名称/路径/id 修改后查询一致 | A-BE / P0 | v012_fts_maintenance.rs；migrations.rs |
| IDX-02 | 收藏/使用时间变化仅写对象行，正常重开复用完整路径索引 | A-BE / P1 | v012_fts_maintenance.rs；schema.rs |
| VIS-01 | 同对象多组件共享请求，最多四个进行中请求，离屏撤销排队 | A-UI / P1 | src/lib/itemVisualCache.spec.ts |
| VIS-02 | TTL 过期时仍有订阅，再挂载第二个组件；全部订阅接到同一最新结果 | A-UI / P1 | TTL 与活跃订阅组合待补；itemVisualCache.ts |
| VIS-03 | 请求期间刷新、切柜、修改自定义图标、删除对象、连续失效重试 | A-UI/E2E / P1 | 刷新/过期响应已有测试；跨界面组合待补 |
| VIS-04 | 同输出路径并发、已存在文件、失败清理、磁盘满、写入被占用 | A-BE/S / P0 | shell_thumbnail.rs 的并发发布测试；I/O 故障与跨进程竞争待补 |
| VIS-05 | 半透明/传统掩码、视频首帧、缺编码器、Shell 慢调用、句柄释放 | A-BE/S / P1 | shell_thumbnail.rs、icon_service.rs；资源泄漏/长耗时场景待补 |
| SET-01 | 分区首次访问才加载，往返保留草稿；重开、保存失败及禁用态一致 | A-UI/E2E / P1 | SettingsPanel.spec.tsx、SettingsField.spec.tsx、TagEditors.spec.tsx |
| SET-02 | 同 id 自定义主题重载、模式偏好、超时初始化与用户即时选择竞态 | A-UI/E2E / P1 | useTheme.race.test.tsx；同 id 元数据/变体刷新待补 |
| ACC-01 | 隐藏区块、fieldset disabled、空控件弹窗、嵌套 portal 的 Tab/Esc/恢复焦点 | A-UI/E2E / P1 | useFocusTrap.spec.tsx、ModManagerPanel.spec.tsx；实际 WebView2 复验 |
| CLI-01 | 无库、损坏库、残缺 schema、未来版本；握手与只读/写入命令返回明确结果 | A-BE/S / P0 | cli/mod.rs、cli/mcp.rs；真实子进程矩阵待补 |
| CLI-02 | 中文/空格路径、参数边界、缺 id、空批次、部分失败、--json 与退出码 | S / P1 | src-tauri/src/bin/tl.rs；每个命令通过实际二进制运行 |
| CLI-03 | GUI/CLI 同时写入、长事务、迁移和只读查询；数据一致、错误明确且可恢复 | S / P0 | 独立共享测试库与受控并发子进程待补 |
| MCP-01 | initialize/ping/tools/list/tools/call、通知、坏 JSON、未知方法、EOF、工具错误 | A-BE/S / P1 | cli/mcp.rs 部分单测；完整 stdin/stdout/stderr 子进程测试待补 |
| MCP-02 | 描述的 schema、返回结构与协议版本一致，工具清单以 tools/list 为准 | A-BE/S / P1 | 协议主规范核验 + 客户端联调；仅测试数据 |
| TUI-01 | 中文宽字符、终端缩放、选择/搜索/退出；正常和异常退出恢复终端 | A-BE/M / P1 | cli/tui.rs 宽度单测；交互终端场景待运行 |
| PKG-01 | 全新克隆构建、真实 sidecar、x64/ARM64 可执行文件匹配 | S / P0 | ensure-cli-placeholder / prepare-cli-bin / release.yml |
| PKG-02 | 安装与便携覆盖升级、重定向、用户扩展、CLI 与 GUI 共享库 | — | **永久取消**（2026-09-17 作者裁定：现在不要、以后也不要实机安装/升级/卸载矩阵）。清单与哈希归 PKG-01 / T6 |

## 16. 性能与视觉复验矩阵

性能比较同时记录数据规模、标签关系、文件类型、磁盘、系统版本、build 类型和缓存状态。每个版本预热后测至少 5 次，报告中位数和完整样本；长任务另做至少 20 次重复以观察分布。数值阈值为计划目标，执行后再由实际结果评估。

| 场景 | 数据与操作 | 指标与验收 |
|---|---|---|
| 启动 | 空库、1k/10k/50k 对象；冷/热图标缓存 | 首个可交互时间、列表返回、CPU/内存、长任务；相同环境连续两批中位数退化超过 15% 时定位原因 |
| 搜索 | 名称/标签/同义词/表达式/拼音/英文容错；首输与后续输入 | 全部匹配 id 及排序相同；10k 热查询目标中位数 <50 ms；端到端单列防抖和渲染成本 |
| 批量写入 | 5k 对象×10 标签、收藏、归档、删除 | 总耗时、锁持有时间、写入行数；失败回滚、标签顺序、幂等保持 |
| 图标 | 10k 库滚动首屏→末尾→首屏，重复十轮 | 请求/排队/缓存数量、峰值并发、GDI/COM 句柄与内存趋势；上限区分活跃订阅和可淘汰缓存 |
| 对账与扫描 | 同盘改名、失效对象、同大小文件密集目录、慢/离线盘、预算边界 | 枚举和哈希次数、时间、DB 响应；用户得到准确的完成/失败反馈 |
| 主题/图谱 | 8 套主题；200/1000 个标签及多父关系 | 切换/选择耗时、布局读取、DOM 数量、字体完成后连线和截图稳定 |

视觉矩阵覆盖 800×600、1200×800、1920×1080 的桌面窗口，另用 390 px 浏览器宽度检查适配；100%、125%、150%、200% 缩放与减少动态效果模式。范围包含主视图、六个设置区块、菜单、预览、编辑器、图谱、通知、加载/空/错误/禁用状态。正文 4.5:1、大字 3:1，图标与边界按实际用途检查；透明叠层及所有控件状态按渲染结果取值。

## 17. 已有证据与本轮执行登记

当前存在以下本机记录，状态为“已留存，待按基线复验”。原生、视觉与性能临时脚本的仓库化工作见 [后续计划](后续计划.md) F01。

| 证据 | 已记录结果 | 适用范围 |
|---|---|---|
| `.tmp-test/final-frontend.log` | 173 项逻辑、88 项交互通过 | 当时工作树；当前基线需统一重跑 |
| `.tmp-test/scan-search-rust.log`、`.tmp-test/final-domain-tests.log` | 全量 197 项通过，后续定向 18 项通过 | 定向用例与全量有重叠，分别记录 |
| `.tmp-test/final-e2e.log` | 62 项断言、68 张截图 | demo 数据与浏览器行为 |
| `.tmp-test/native-perf-result.json` | 原生搜索、标签拖拽、设置草稿、焦点循环、主题模式通过 | debug、1k 测试文件；安装包和外部资源管理器拖入仍需单独验证 |
| `.tmp-test/theme-source-check.json`、`.tmp-test/ui-panels-probe/result.json`、`.tmp-test/overlay-contrast.json` | 252 个色值、108 个设置组合、72 个浮层组合核验 | 本机设计源与被测状态；状态覆盖见 §16 |
| `.tmp-test/search-hot-before.json`、`.tmp-test/search-hot-after.json` | 10k 混合数据，复杂热查询约 96→23 ms | 同机样本结果；release 与首次输入另测 |

- [x] T0：`b2bb292` 干净工作区，app `1.8.0`，schema 14。证据 `.tmp-test/evidence/e2e-b2bb292/recert.json`；自动层清单 `.tmp-test/evidence/2026-09-17T08-22-21-262Z`。
- [x] T1/T2：`npm run verify:candidate -- --shots` 的 test:all 通过。tsc 1/1 · 逻辑 226/226 · vitest 119/119 · cargo lib 148/148 · 集成 78/78。`npm run build` 通过。
- [x] T3：必须直接跑 `node scripts/demo-screenshots.mjs`（80/80，62 张 → `.tmp-test/evidence/e2e-b2bb292/ui`）。官方 `verify:candidate --shots` 在 Windows 上 `spawnSync(..., {shell:true})` 会以 `3221226505`（0xC0000409）秒退，空 log；产品断言本身绿。
- [x] T4：本轮重建的 release exe（mtime 16:26，SHA256 `F64BCB5E…5B647C`）原生冒烟通过。1000 项 `list_ms≈109`、`search_ms≈188`；草稿/拖拽/搜索/画廊/设置焦点全 true；图标缓存已在沙箱 `Save/item-icons`。证据 `.tmp-test/evidence/e2e-b2bb292/native-result.json`。
- [x] T5：未改搜索算法或拆 bundle；10k/50k 按确认不做。`performance.rs` 含在集成 78/78 内仍绿。
- [x] 数据目录矩阵：`python scripts/e2e-data-migration.py --exe src-tauri/target/release/tag-launcher.exe` → A–H **13/13**，升级后 schema=`14`。
- [x] T6：本轮不重跑打包。上次 x64 `npm run pack` 仍有效：NSIS SHA256 `53f0fbfd…656aa266`；便携 zip SHA256 `fcd7dcde…b877e325`。ARM64 归 CI。安装/升级/卸载实机矩阵已永久取消。

每次执行记录：`用例 ID / 提交 SHA / 环境 / 夹具 / 命令或操作 / 退出码 / 预期与实际 / 状态 / 日志或截图 / 关联问题 ID`。测试失败先保存复现证据，再进入 [审阅计划](审阅计划.md) 的问题闭环。

## 18. 性能矩阵基线（2026-09-21，第四轮整改后）

设施：`python scripts/perf-matrix.py --counts 1000,10000,50000 --repeat 3 --rows-only`（隔离沙箱 + CDP 驱动真实 WebView2，合成夹具单事务批量建行；50k 用 `--rows-only` 只建行不建文件）。报告 `.tmp-test/perf/<sha>/report.json`；`--baseline <旧报告>` 对比，中位数退化 >15% 退出码 1。

测时口径：get_items 一律**页内计时**（payload 经 CDP 回传会产生秒级伪差，50k ≈ 17MB——曾误判为 IPC 慢，实为此伪差）；`reconcile_ms` 为 `reconcile_items {wait:true}` 同步对账；theme_switch 为点击→`data-scheme` 翻转+两帧（含 240ms 设计过渡与色位补丁，与库规模无关）。

| 指标 | 1k | 10k | 50k | 阈值 |
|---|---|---|---|---|
| startup_tti_ms（进程起到状态栏计数就位） | 1485 | 1531 | 1610 | 2000/3000/8000 |
| list_ms_cold（首调，页内） | 6.3 | 47.9 | 228.9 | — |
| list_ms_warm（对账落定后中位，页内） | 6.4 | 44.9 | 229.5 | 50/300/1500 |
| reconcile_ms（同步对账） | 16 | 63 | 296 | — |
| search_cold_ms（首次击键含索引构建） | 172 | 203 | 360 | 300/800/4000 |
| search_hot_ms（热击键含 150ms 防抖） | 172 | 188 | 203 | 200/250/600 |
| theme_switch_ms（含过渡与补丁） | 562 | 593 | 594 | 600（记录线） |
| scroll（avgFps / jank%>50ms） | 210 / 0 | 172 / 0 | 131 / 0 | ≥55 / <5% |

第四轮整改要点对应证据：get_items 纯读化后 50k 温读 229.5ms（整改前同路径内嵌全量对账）；切主题经 ITEM_TAG_COLORS_PATCH_EVENT 就地改色，50k 仍 ~0.6s（整改前为 applyTheme + 全量重取 + 图标重取 + 全网格重渲染的级联）；搜索索引 1000 条/片 × 250ms 间隔预热（上限 5000 条），首分钟不再整片冻结主线程。

已知边界：前端全量驻留数据模型下，50k 全量 loadAll 的 IPC 载荷 ≈ 17MB（页内 229ms 为纯读与反序列化下限）；窗口化数据层（按需分页加载）属后续架构项，不在本轮。
